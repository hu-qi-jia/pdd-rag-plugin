# Dependencies
node_modules/
.pnpm-store/

# Build outputs
build/
dist/
.plasmo/

# Environment
.env
.env.*
!.env.example

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.tmp

# Testing
coverage/

# TypeScript
*.tsbuildinfo

# 根目录的 Spec-Kit 工作目录(上游遗留)。**必须锚定**:裸的 `specs/` 会连 `docs/specs/` 一起吞掉,
# 而那底下是本项目的设计文档(工作状态、plans、scripts/package.mjs 都在按路径引用它们)——
# 2026-09-17 才发现两篇设计文档一直没入库,克隆下来的仓库找不到被引用的路径。
/specs/
.diag-fresh-profile/
.cursor/
.claude/
.specify/

# Embedding models
assets/models/

.worktrees/
# 一次性诊断 profile(自动化用,勿入库)
.diag-golden-profile/
.diag-run-*/
# scripts 硬编码绝对路径在 bash 下被吞反斜杠产生的乱码 profile 目录(防误入库)
*chrome-debug-profile/
*.diag-run-*
/pdd-cs-quick-reply-*.zip
