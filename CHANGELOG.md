# Changelog

本扩展(拼多多客服快捷回复)的变更记录。格式参考 Keep a Changelog,版本号即 package.json 版本。

> 上游 Personal AI Memory 的历史变更见 `docs/upstream-CHANGELOG.md`(Apache 2.0);
> 逐轮开发明细见 `docs/工作状态-2026-09-08.md` 与 git log(每轮均有 docs+test 提交)。

## 0.15.0 — 2026-09-17

### 变更
- **整合行填完即退场**(第五十一轮,用户"当用户使用快捷键调出推荐回复面板后,选择『根据知识库...』后,填充内容到输入框后面板退出"):生成结果填进输入框成功 ⇒ 面板**自行退场**,与候选行拉平(点一条候选填进去,面板同样自己走,不留着挡在刚填好的输入框前面),退场前 toast 交代「已整合并填充:知识库 · 请手动发送」。**填不进去**(页面没有输入框)则**不退场** —— 那种情况下结果只在行里,用户要从这儿手动复制,「重新生成」也得留在眼前
- 整合行终态文案「✓ 已填入输入框」→ **「✓ 已生成」**:填成功的那一路根本看不到这一帧,而剩下来能看见它的那一路("没输入框")说"已填入"就是假话;填入与否由 toast 交代
- **关闭钮收成裸 ×**(用户"推荐回复面板右上角的关闭按钮,仅保留 × 图标即可"):撤掉 v2.7.0 加上的常驻灰圆底 —— 在一屏全是无底文字的极简面板里,那颗圆是唯一多出来的墨点。命中区仍是 26px(视觉变小,可点范围不变),悬浮时软底回来,"点得动"由悬浮回执交代;v2.7.0 那条"关闭入口要常驻可见"仍成立 —— **常驻的是 × 本身,不是它身后的圆**

### 项目结构审查(第五十一轮,用户"以架构师的身份看一下项目结构是否有问题,并修复")

按"分层契约 / 单一真源 / 死代码 / 注释与文档是否还成立"四条轴扫了一遍全库。**分层没有塌**:popup 与 contents 互不引用,ui / pdd / shared / types 四层零反向依赖。修掉的是下面这些"契约没兑现"的地方 —— 全部是收敛,没有新增概念:

- **协议在两端各抄了一份**:内容脚本手写 GET_STATS/GET_SUGGESTIONS/ADD_GOLDEN/DELETE_GOLDEN 四个响应形状、`background/index.ts` 手写 `PDD_FILL_INPUT` 的响应形状、内容脚本手写请求形状 —— 而 `types/messages/` 里这些类型**都有**。现全部改取协议类型(`GetStatsResponse` / `GetSuggestionsResponse` / `AddGoldenResponse` / `DeleteGoldenResponse` / `ContentFillMessage` / `ContentFillResponse`):后台改字段,前端当场编译不过,而不是运行时静默拿到 undefined
- **`ContentFillMessage`/`ContentFillResponse` 此前是"定义了但没人用"的类型**(SW → content 这一跳的唯一契约)。不是删掉它们,而是让**两端都用上** —— 空定义是文档,用上的定义才是约束
- **类别中文名抄了 5 份**,而且**徽标那份和提示那份说的不是一个词**(徽标「历史」/ 提示「历史回忆」):抽 `pdd/ui-logic#kindLabel` 单点,两处同词(取设计文档 §八 的「历史」)。同一轮还发现**错误文案表漏了 `no body` 这一个标签**,`http 4xx` 又只有兜底句 —— 补 `AI_ERROR_TEXT['no body']` 并按 4xx/5xx 分开兜底(`aiErrorText()`):用户现在能分清"地址/密钥不对"和"对方服务挂了"
- **`AI_PORT_NAME` 常量存在但 `background/aiIntegrate.ts` 里硬编码了字面量**:改取常量(名字改了不会两边不同步)
- **删死代码**(先用全库 grep 逐个确认零引用,再删):`ui/components#SectionLabel`、`ui/icons#AiSparkIcon`(第五十轮删 ✦ 后留下的孤儿)、`shared/message-passing#sendMessageFireAndForget` 及其实测、`background/db#clearErrors` 与 `#getGoldensByFolder`。同时把**假话注释**改真:`background/index.ts` 那句"全文件仅此一处 cast"实际有 7 处,现如实说明每处的理由
- **`pdd/llm-draft.ts` 迁到 `popup/llmDraft.ts`**:`pdd/` 那一层的契约写在同目录文件头("纯逻辑,不触碰 chrome/Dexie"),而这个文件从头到尾只有 chrome.storage 的读/写/删,唯一使用者就是设置页。纯逻辑那一半(`pdd/llm-form.ts`)原地不动 —— **层契约现在靠目录就能读出来**
- **两处重复实现合成一份**:内联二次确认行此前文件夹页手搓一对 `<button>`、记忆页直接用通用 `Btn`,于是同一个交互在两页里**字号不同(10.5 / 11.0)且按钮档位不同(24 内联档 / 26 表单档)**,记忆页那份还写着 `fontSize.caption + 0.5` —— 正是 `design.ts` 明令禁止的临时值。抽 `ui/components#ConfirmRow` 单点,统一取**内联档**;`ICON_SIZE` 的 13 也曾在 popup 与覆盖层各写一份,现同取 `design.size.icon`
- **令牌没兑现的地方补上**:覆盖层里写死的 `rgba(184,134,11,.14)` / `rgba(20,174,92,.12)` / `rgba(217,48,38,.12)` 三个软底,色值其实全是 `semantic` 里那几个色的另一份拷贝(改色必漏一处)→ 立 `goldenSoft` / `knowledgeSoft` / `dangerSoft` 三个令牌;关闭钮的 `26px` → `controlH.form`;`:root` 里 `.pddcs-btn` / `.pddcs-input` 写死的 `font-size: 12px` → `fontSize.body`(设计规范里"按钮"本来就该是 12.5,覆盖层那颗「AI回复」钮的注释还写着"与 popup 的 .pddcs-btn 同档…12.5px 字号"—— 两边其实一直差 0.5px,现在那句话成真了);`size.railBtn` 令牌存在但 popup 里写死 36px → 改取令牌
- **过时注释/文档修正**:`retrieval.ts` 文件头还写着旧配额(历史 2 / 知识库 1,实际 3/3/3)、`types/memory.ts` 的类型注释还在说"Shift+同键 = 上一候选"(第二十四轮已删)、README 的面板组成与「数据仅存本机,不上传任何服务器」、设置页「关于」卡同一句

### 数据边界(本轮唯一涉及文案的改动)
- 设置页「关于」与 README 的合规口径**补上 ADR-0006 的条件限定**:原先那句"不上传任何服务器"在「AI 整合」开启并手动点击后**不再成立**(ADR-0006 开了一条窄豁免)。现改为**默认不联网** + 把三条同时满足的条件、外发内容范围、历史/标准回答永不外发、结果只填不发,一并写清 —— 与 ADR-0006 的「外发内容仅限」同源同义;无遥测、无日志上报一句保留

### 验收
- 单测 **522/522**(46 文件):关闭钮静止 `background: transparent` + 命中区 26px + 两主题悬浮回执、done 文案说"已生成"且不含"输入框";新增 `kindLabel` 三类取名(并断言「历史」不含"回忆")与 `ConfirmRow` 五项(文案/回调/内联档高度/字号落在令牌阶梯/危险实底与描边幽灵)
- `verify-ai-integrate.mjs` **51/51**(此前 44):新增「填成功 → 面板自行退场且 toast 交代」「填不进去 → 不退场、文案 ✓ 已生成」「关闭钮静止无底 + 命中区 26px」,并补了**键盘整条路径**(Ctrl+Enter 开面板 → Tab 走到整合行 → Enter → DELTA/DONE → 自行退场且文本落进输入框);⑪「生成后的展示」一组改在"页面无输入框"那条路上验(填成功就关面板了,那条路才看得到终态那一版)
- `verify-ai-popup.mjs` **50/50**(关闭钮一条改判静止无底 + 新增悬浮回执)、`verify-ai-button-geometry.mjs` 13/13、`verify-settings-ui.mjs` 33/33
- 结构改动后**全量复跑**:`verify-p3` 21/21、`verify-kb` 19/19、`verify-kb-doc` 11/11、`verify-golden-multi` 17/17(`verify-p2-ui.mjs` 是面向真实站点的登录态脚本,本轮未跑 —— 它要独占 `.chrome-debug-profile`,浏览器已开着时起不来)
- `npx tsc --noEmit` 0 error;`npm run build` 通过,manifest `0.15.0`,bge 模型随包拷贝

## 0.14.0 — 2026-09-17

### 变更
- **推荐面板字型收成「三档字号 + 两档字重」**(第五十轮,用户"规范一下字号和权重"):此前面板里散着**五档字号(10.5 / 11.5 / 12.5 / 13.5 / 15)+ 三档字重(400 / 500 / 600)**,相邻两档只差 1px 又说不出各自存在的理由。现由 `design.ts#panelType` 单点给三档 —— 标题 **15** > 内容 **13.5** > 辅助 **11.5**(等差 2px),字重只剩 **400 / 600**(500 在面板里退役);面板任何文字都必须落在这些档位里,没有第四档,也不许 `±0.5` 的临时值
- **整合行的说明行与生成结果统一到同一档 13.5px**(用户"小字和生成后的文字字号不同"):此前说明行 11.5、生成结果 13.5,点下去整行的小字换成大字会"跳"一下,像换了个控件。现在整行只有一个字号,主次靠字重分(动作名 600 / 说明行 400)
- **整合行的说明文案简化**(用户"说明文案再简化"):两行从「点击后把命中的 N 条知识库内容发给你配置的接口 / 只发送这条买家问题,历史回复与标准回答不出本机」改为「点击后**只**发送这 N 条知识库内容与买家问题 / 发给你配置的接口;历史与标准回答不出本机」—— 外发清单(带条数)、去向、不出网声明三件事一件没少,只做了减法:去掉"命中的"这类限定语,两句合成一行,两行都在 311px 宽度内不折行
- **删掉整合行行首的 ✦ 图标**(用户"删除图标"):整块知识库绿底已经在说"这是一次动作",装饰符号只是噪音,还会把主文案挤窄;在途反馈交给主行文案与第二块的流式正文,✦ 的转圈动效与它的 `prefers-reduced-motion` 分支一并删除

### 说明
- **「整合只用了一条」不是缺陷**:外发链路本来就是"面板上展示了几条知识库候选就发几条"(至多 3 条,`PANEL_QUOTA.knowledge`),`pdd-ai-button.ts` 取的是 `items.filter(kind === 'knowledge')` 的**全部** id。若行内显示「1 条」,含义是这一轮只有 1 条知识库块过了 `kbThreshold`(默认 0.4),不是"取了第一条"。本轮把这条口径用测试锁住:单测断言 3 条候选 → 3 份材料、提示词里一份不落;e2e 用 3 条知识库候选真跑一遍,断言 `knowledgeIds` 三家齐全且顺序与面板一致
- 面板之外不受新字型表约束:页面上的「AI回复」悬浮钮(12.5 / 500)与 `.pddcs-toast` 各有自己的语言,不随面板走

### 验收
- 单测 **517/517**(45 文件):新增「说明行与生成结果同档同排版」、面板字型只有三档字号与两档字重(扫全部面板规则,任一 `font-size`/`font-weight` 越档即失败)、✦ 图标与转圈规则已从 CSS 中消失、材料配额内 3 条候选全部并入
- `verify-ai-integrate.mjs` **44/44**(新增 3 条知识库候选 → `knowledgeIds` 三家齐全且不夹带标准回答、说明行与候选正文同档 13.5、行首无 ✦ 且主行只剩动作名 + 重试钮)
- `verify-ai-popup.mjs` 49/49、`verify-ai-button-geometry.mjs` 13/13、`verify-settings-ui.mjs` 33/33

### 边界
- 整合行"什么出去 / 什么留下"的两条信息与 ADR-0006 的「外发内容仅限」同源同义,本轮只做减法:外发清单(含条数)、去向、不出网声明三件事一件未少,ADR 里的引文已同步更新
- 行序与配额、点击填充、键盘导航跳过整合行、打开面板不发任何请求,一字未动

## 0.13.0 — 2026-09-17

### 变更
- **推荐回复面板整体重设计:从"工具风卡片"改为 Apple 式磨砂浮层**(第四十九轮,用户"不要带着现有设计的框架,大胆一点,简约风格,类似 apple 的设计")。四处根本差别:①**材料** —— 面板不再是"一块不透明白面 + 1px 描边",改为**半透明底 + `backdrop-filter: saturate(180%) blur(20px)`**,让平台页参与成像;材料的不透明度有下限 **0.75**(再低就是"看得见后面文字"的半透明,是用户反馈过的观感;材料感来自模糊,不靠把面板做虚),引擎不支持 `backdrop-filter` 时自动退回实心面色(判断单点 `overlay-css#panelSurface`,内容脚本的内联兜底与静态 CSS 同源);②**描边全去** —— 头/尾的分隔线一起撤掉,三段只靠留白分层,面板与页面之间唯一的硬边是阴影第一段那圈 **0.5px 亮环**(浅色黑环 / 深色**白**环),阴影改"亮环 → 近影 → 大范围环境影"三段;③**圆角同心** —— 面板圆角 12 → **20px**,行圆角不再写死,由 `radius.xl − ROW_INSET_X` 推出(20 − 8 = **12px**),「改外壳圆角、行圆角自动跟随」;④**留白单源** —— 行内缩 8 / 行内边距 12 / 上下 10 / 行距 6 四个常量单点定义,面板标题与页脚的左右内边距由 `行内缩 + 行内边距 = 20` 推出,于是**标题左缘与行内容左缘在同一条竖线上**(旧版差 2px,量 `rect.left + paddingLeft` 才看得出来)
- 面板的三枚小控件(关闭钮 / 行内图标钮 / 重试钮)统一换语言:从"描边方钮"改为**无描边 + 全圆端 + 安静填充**(`fillQuiet`),关闭钮不悬浮也常驻可见(浮层的关闭入口不该要用户先找到它);「推荐回复」标题从与计数挤在同一串 12.5px 灰字里,拆成 **15px 半粗标题 + 12.5px 三级灰计数**两档
- 整合行(「根据知识库内容整合并回复」)撤掉同色描边,只留知识库绿软底 —— 新面板里候选行静止时**都没有底**,"有底/没底"这一条已足够把它与素材区分开,再加描边是多余的重量;绿底改**按主题各给一档**(浅 0.09/0.16,深 0.22/0.30):深色材料压在浅色平台页上合成出来的是中灰而不是近黑,原来那一份固定 9% 的绿到那上面只剩一点色偏,"这是本面板唯一的动作"就立不住了
- 入场动效从"160ms 淡入上移"改为 **220ms `cubic-bezier(.32,.72,0,1)` 淡入上移 + 98% 缩放**(`prefers-reduced-motion` 下关闭)

### 修复
- **面板定位按"矮了一截"的高度算,底边压出视口**(第四十九轮,本轮新动效引入后由 e2e 抓到):`getBoundingClientRect` 返回的是**变换后**的矩形,入场动效那 2% 缩放让刚挂载的面板量出 378 而非 385.78,`popupPosition` 便按矮的那版夹边界,动画结束回到原尺寸时底边落到 `bottom 798.78 / vh 800`。现量高期间 `animation: none`、量完还原(旧版动效只翻译不缩放,所以这个坑一直没露头)

### 验收
- 单测 511/511(新增「Apple 式材料重设计」一组 9 项:材料令牌与 `-webkit-` 前缀、两条硬约束、`panelSurface` 单点、无描边 + 0.5px 环、全篇无 hairline、三枚小控件的胶囊语言、标题与行的竖线对齐、页脚居中、入场动效与 reduced-motion)
- `verify-ai-popup.mjs` 扩到 **49 项**,全部按真实 `rect`/`computedStyle` 量:材料与模糊半径、α 区间、无描边、环、圆角 20、标题字档与左缘、行圆角 12 与内缩 8、行距 = 行内节奏 = 6、头尾无发丝线、页脚居中
- `verify-ai-integrate.mjs` 37/37、`verify-ai-button-geometry.mjs` 13/13、`verify-settings-ui.mjs` 33/33

### 边界
- 本轮**只换外观**:行序与配额、点击填充、键盘导航跳过整合行、打开面板不发任何请求、ADR-0006 的外发边界文案与实现,一字未动

## 0.12.2 — 2026-09-17

### 变更
- **推荐面板的整合行重做成「动作卡」**(第四十八轮,用户"高度太小了,修改为和词条高度类似,视觉效果要和词条区分开"):原先只有一行 11.5px 绿字、整行约 25px,夹在 74.6px 的词条之间像一条漏排的标签。现在沿用词条同一套盒子、行内补齐第二层,实测 idle **72.5px ↔ 词条 74.6px**;视觉上从"中性面色 + 类别徽标"(素材)改成"知识库绿软底 + 同色描边"(动作),失败态整行转红
- 整合行的第二行是说人话的**数据边界**:「点击后把命中的 N 条知识库内容发给你配置的接口 / 只发送这条买家问题,历史回复与标准回答不出本机」—— 既撑起高度,又把 ADR-0006 的边界写在点击之前,且这句话不会被省略号截断
- **生成后的展示按正文口径重排**:结果不再当"行内小字",改与候选正文同为 13.5px / 1.6 / 4 行截断;「重试 / 重新生成」钮从行尾另起一行搬进主行右端(24px 行内档),不再自占一行
- 整行点击**只在未点过时生效**:出结果后想选中/复制结果的人点一下文字,不会因此再花一次 API 调用(重试走右上角那枚钮,键盘 Enter 等同于点它)

### 验收
- `verify-ai-integrate.mjs` 扩到 38 项:新增按**真实 rect / computedStyle** 量的几何与配色断言(行高同档、内容左缘重合、绿底描边、字号同档、出结果后点整行不再发请求),外加 idle / 生成后两张版面留档

## 0.12.1 — 2026-09-17

### 修复
- **AI 接口配置保存后内容全消失、整块不可用**(第四十八轮,用户报告):表单字段叫 `baseUrl/apiKey/model`,设置字段叫 `llmBaseUrl/llmApiKey/llmModel`,保存时按前一组名字提交 —— 后台按后一组读,一个字段都没落库;回填又拿后台返回的旧设置覆盖表单,当场清空输入框,「测试连接」变灰。字段名映射现收敛到 `llm-form.ts` 的 `llmSettingsPatch`/`llmFormFromSettings` 两个函数,不再有第二处手写映射
- 保存时掐掉挂起的草稿落盘:保存后草稿是被清掉的,若 300ms 防抖定时器还在,它会把旧值写回去 —— 重开时框里是"复活的草稿"而非刚保存的配置,且与后台夹取后的正式配置永远差一点(「有未保存的修改」怎么点保存都消不掉)

## 0.12.0 — 2026-09-17

### 修复
- **问答体 .md 文档检索不到**(第四十八轮,用户报告):分块器只按 `#` 标题切,整篇问答被并成一块,向量被多个主题平均稀释,余弦相似度跌破阈值后在排序**第一步**就被丢掉,BM25 根本没机会参与 —— 表现为"知识库明明有这条,问起来永远匹配不上"。现按问答对切分(同义前缀 `Q/问`、内联 `A/答` 均识别),实测「防水吗」相似度 0.391 → **0.709**,11 块全部命中且 top-1 正确
- 删掉文档最后一块时同步清理原文,避免下次重分块把整篇"复活"(第四十八轮)

### 新增
- **AI 整合**(第四十八轮,默认关闭):推荐面板的知识库候选上方多一行「根据知识库内容整合并回复」,**点击才**把本轮命中的知识库内容与买家问题发给你自己配置的接口,流式回填输入框;发送仍由人工完成。设置页含接口地址/密钥/模型/超时 + 「测试连接」(401/403 报密钥、404 报地址或模型名、429 报额度),密钥只存本机、不同步云端。生效条件见 `docs/adr/0006-optional-llm-integration.md`:不配置、不开启时扩展保持完全本地
- 知识库文档原文入库(kbDocs 表):分块规则再变时可自动重切,**不必手动重传文档**
- 知识库页对**升级前**上传的文档给出重新上传提示(那些文档没有原文,自动重切够不着,不提示就会静默失效)
- AI 接口表单边填边存:弹窗失焦被 Chrome 销毁后重开,已填内容原样还在(草稿与生效配置分键存放,未点保存的半截密钥不会变成会出网的配置)

### 变更
- 设置页开关**只有开关本体可点**:原先整行是 `<label>`,点配置文字也会翻转,而文字旁边就是说明文字,想选一句复制都做不到
- 验收脚本 `verify-ai-integrate.mjs`:整合行 25 项(headless,无网络,LLM 桩在 Port 后面);`verify-settings-ui` 跟进开关 DOM 变更

## 0.11.0 — 2026-09-17

### 新增
- 向量模型内置:bge-small-zh-v1.5 量化 ONNX + tokenizer 随扩展包分发(ADR 0005),安装即用、离线可用,首跑不再下载 ~25MB;`pnpm fetch-model` 构建期拉取命令(锁 revision `75c43b0`,hf-mirror 仅存在于构建期)

### 变更
- offscreen 嵌入改纯本地加载(`allowRemoteModels=false` 永久),本地模型失败给出「重新安装扩展」可行动文案
- verify-kb e2e 封锁 hf-mirror 作离线证明(19/19 照常通过);设置页合规文案同步(「模型已内置,离线可用」)

### 移除
- `host_permissions` 的 `https://hf-mirror.com/*` —— 扩展运行期零远程请求

## 0.10.0 — 2026-09-16

### 修复
- 编辑金标准问题后检索缓存未失效,旧问题锚/旧向量继续参与检索直至 SW 重启(第二十七轮)
- 知识库文档整篇替换「先删旧后写新」无事务,中途失败丢失整篇旧文档(第二十七轮)
- 嵌入回填改写 updatedAt,致标准回答面板/知识库列表「最近设置靠前」在慢机器上随回填完成顺序漂移(第三十一轮,数据层 e2e 进 CI 后暴露)

### 工程审查(第二十七轮起)
- SW 消息路由 24 分支 switch 改类型化 handler 映射表,消除逐 case 的 `as` 强转
- manifest 配置收敛到 package.json 单处(原 config 文件优先,双份有静默漂移风险)
- scripts/ 抽公共底座 `lib.mjs`:路径相对化 + 环境变量覆盖,归档一次性探针/诊断脚本
- `.gitignore` 覆盖乱码 profile 目录,防登录态误入库
- 中期重构(第二十九轮):建 `src/shared/`(跨上下文基础设施+常量)与 `src/pdd/`(平台领域纯逻辑),types/ 回归纯类型,embedding 引擎归位 offscreen —— 后台 bundle -85%;utils/ 与 504 行 messages.ts 按域拆清
- 数据层 e2e 进 GitHub Actions(第三十一轮);scripts 底座去硬编码 chrome 路径(`channel: 'chromium'` 支持无头加载扩展,`PDD_E2E_CHROME` 可覆盖)
- FoldersTab 操作流补 11 例特征测试;pdd-ai-button 覆盖层几何(行可视/按钮与弹窗定位/滚动校正)提取为 5 个纯函数,TDD 18 例
- ADR 0004:检索管线不包化,目录分层即边界

### 界面(第二十五~二十六轮)
- 审查修复轮(第四十六轮):popup 五个主题变量由"内联桥 + effect"双轨统一为 `<html>` 上一条 layout-effect 桥,变量**首帧前**落位 —— 深色主题开 popup 首帧不再闪白角;`html, body` 底色移除无竞争对象的 `!important`,fallback 插值 `lightTheme.bg` 令牌单源;DESIGN §九旧版外框条目(radius 8px/body 透明)与 `tk.shadow` 令牌注释更正为现行口径
- 去掉 popup 外框阴影 + 文档底随主题(第四十五轮):popup 外层容器不再挂 `boxShadow`(24px 圆角放大后,阴影溢进窗口四角,视觉上像一圈半透明边框);`html, body` 底色由 `transparent !important` 改为 `var(--pddcs-page-bg)`(App 用 effect 写入 `tk.bg`,避免深色主题下四角露白);浮层阴影令牌 `tk.shadow` 保留给聊天页推荐面板
- 操作组一律常驻 + 图标钮左缘对齐正文(第四十四轮):文件夹页(标准回答行四枚图标钮、标题栏重命名/删除)与知识库页(填充/复制/编辑/停用/删除)的操作组**默认可见**,`.pddcs-row-ops` 悬浮显隐规则与 `.pddcs-row` 钩子从 RESET_CSS 删除、三处类名摘除;`FoldersTab#opsWrap` 对齐档细分 `right`/`text`/`icon`,靠左的**图标钮组**用 `marginLeft: -(controlH.inline−ICON_SIZE)/2 = -5.5px` 抵消 24px 钮盒内的居中留白,使**图标左缘与上方正文左缘对齐**(文字钮与下拉走 `text` 档 0 偏移;图标尺寸收敛为 `FoldersTab#ICON_SIZE` 单点)
- 知识库卡片操作组收进悬浮层(第四十三轮):原「填充」常驻主钮 + 右侧顶边的次级组(复制/编辑/停用/删除)合并为一个悬浮层组,顺序 `填充 → 复制 →(编辑)→ 停用|启用 → 删除`,靠左紧贴、与文件夹页操作组同构(删除二次确认时整组常驻);填充逻辑未动
- 文件夹词条操作组左移 + popup 主面板大圆角(第四十二轮):标准回答行删掉常驻「填充」主钮(填充能力仍在聊天页推荐面板与知识库页),四个 24px 图标钮(迁移/复制/编辑/删除)左移至原填充位(`opsWrap` 新增 `alignLeft`);`radius` 重排为单调阶梯 4/6/8/12/24 —— `xl` 归聊天页推荐面板(12px,原 `xxl`)、`xxl` 归 popup 主面板外框(**24px**,原 `xl = 8px`)
- 设置页字号与间距立规 + 开关滑杆改灰白(第四十一轮):新增 `design.ts#formType`(分组标题 13.5 / 字段标签 12.5 / 说明文字 11.5,等差 1px)与 `formGap`(配置项与卡片之间 16px、标签→说明 4px、标签→轨道 8px),`Card` 标题由 11.5 灰字升为分组标题档;开关选中轨道与滑杆填充由 accent 蓝改 `controlActive`(浅色 #45484D / 深色白),柄色 `controlKnobBg` 与激活色反相
- 浅色主题主色软化(第四十轮):`text` #161616 → **#2B2B2B**;主按钮 `btnPrimaryBg` #161616 → **#45484D**(悬浮 #333 → #53565B)—— 取深色主钮同值,两主题 primary 完全同值;两处均为 `theme.ts` 令牌,popup 与聊天页面板共用故一并生效
- 面板正文左基线改锚点(第三十九轮):「原问题 / 回答」文本改为对齐类别徽标**内文字**左缘(各加 `padding-left: BADGE_PAD_X` = 9px,与徽标水平内边距同一常量),不再对齐徽标外框;行整体左缘不变
- 面板词条留白重配(第三十八轮):标签/原问题/回答两处间距各 +2px(2→4px),同时行内上下 padding 6→3px —— 词条默认高度净减 2px,信息更松、词条更矮;「同内容×n」由行右下角移到**徽标右侧同一行常驻**,折叠条位预留规则删除(折叠与否不再影响行高)
- 面板标签放大 + 操作钮图标化 + 列表收紧(第三十七轮):类别徽标 10→11.5px(内边距 3px 9px/圆角 6px)、下方「原问题/正文」与标签左缘对齐、操作钮由文字改**图标**(星标 + 复制,24px 钮 × 13px lucide 图标,已设态为实心金星)右移 6px、条目正文间隙 20→14px
- 品牌标落地(第三十六轮):用户提供的"黑底 P"图替换扩展图标,popup 导航栏顶部放同源品牌标(24×24 圆角描边)
- 设置页文案清理(第三十五轮):删导入/存储两处说明行、卡片标题去括号注、三条开关 desc 精简——行为反馈回归 Notice 与二次确认
- 候选列表紧凑化(第三十四轮):行距与行内 padding 收紧,条目间视觉间隙 24→18px
- 面板细节四调(第三十三轮):类别徽标软底 chip 化(琥珀/绿/中性灰)/「同内容×n」右下角悬浮才显/标题加粗 600/面板圆角 12px
- 推荐回复面板重设计(第三十二轮):三段式浮层壳(头/页脚常驻,滚动移交中段 body)/ 候选行内缩圆角软行 / 操作钮悬浮才显 / 选中态去 3px 左描边(`selectedBar` 令牌移除)/ 面板加宽 360px / 入场动效;verify-ai-popup 启动方式对齐 lib 约定(`channel: 'chromium'`)
- 推荐面板 ChatGPT 化:去分隔线 / 柔和双层阴影 / 徽标圆点化 / 页脚键帽化
- 候选卡对话式排版 + 字号主次四层级(回答正文为唯一主层)
- 面板循环切换滚动校正:回绕首条回顶,不再被 sticky 头遮盖

### 更早(第二十~二十四轮)
- 检索配额 3/3/3、知识库独立阈值、标准回答同问多答上限
- 面板键盘导航(Tab 循环 + Enter 填充)、细滚动条公共化、面板不透明双保险

明细见 `docs/工作状态-2026-09-08.md` §〇-S 至 §〇-Z。
