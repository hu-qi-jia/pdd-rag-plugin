# 设计规范(Design System)

> 版本 2.6.19 · 2026-09-16 · 对齐 Figma 编辑器工具界面(与 pddddd 控制台同一设计语言;v2.0 的 Figma 营销官网风整体替换)
> 版本口径:本文件 v2.6.x 为**设计系统规范**版本号,与扩展本体版本(package.json,当前 0.10.0)独立计数
> v2.6.19 增补:面板细节四调(第三十三轮,2026-09-16 用户指定)—— ①类别徽标 6px 圆点 →
> **软底色 chip**(标准回答=琥珀软底金字,知识库=绿软底绿字,历史=中性灰软底,更明显);
> ②「同内容×n」移至**行右下角悬浮才显**(折叠行预留底边条位 `.pddcs-cand-folded` 不压正文);
> ③头部「推荐回复」**加粗**(500→600,12.5px 中灰保持);④面板圆角增大 8→**12px**
> (`radius.xxl` 新档,popup 本体 8px 不动)
> v2.6.18 增补:推荐回复面板**重设计**(第三十二轮,2026-09-16 用户"还是不好看,重新设计")——
> 换骨架不换口径:①**三段式浮层壳**(头/页脚常驻成外壳,滚动只发生在中段 `.pddcs-popup-body`,
> sticky 头与"页脚滚走"问题随结构消失,页脚加 hairline 上边 + bgSecondary 底);②候选行
> 通栏矩形 → **内缩圆角软行**(margin 4px 8px + 圆角 8px,悬浮=选中同一软中性灰填充);
> ③**操作钮悬浮/选中才显**(静止 opacity 0 + pointer-events none,9 行候选不再顶着一排
> 常驻灰字小钮,布局占位不变无跳动);④选中态**去 3px 左描边**(`selectedBar` 令牌移除,
> 纯软灰填充);⑤面板 340→**360px**;⑥入场 160ms 淡入上移(prefers-reduced-motion 关闭)。
> 用户已拍板口径全部保留:中性灰选中、不透明、无分隔线、6px 圆点徽标、字号主次、键帽页脚
> v2.6.17 增补:候选卡**对话式排版 + 字号主次**(第二十六轮,2026-09-16 用户"继续 4+5,
> 字号分清主次")—— 问题回显上置为引子(11.5px 灰字单行省略,原底部「原问题:…」来源行移此,
> `.pddcs-cand-src` 规则随之移除);回答正文 = 面板唯一主层(13.5px + 1.6 行高);
> 头部极简(12.5px 中灰 500,原 13.5/600 主色);页脚/迷你钮收至 10.5px caption 档。
> 循环切换滚动校正:sticky 头会盖住 `scrollIntoView(nearest)` 的对齐位(用户反馈"回绕
> 首条被折叠遮盖"),改头部实高感知的手动滚动,**选中首条直接 scrollTop=0**。
> v2.6.16 增补:推荐面板 **ChatGPT 化**(第二十五轮,2026-09-16 用户指定"留白分层 + 近乎单色 +
> 柔和浮起",四项:①候选行去 1px 分隔线,留白 + 悬浮底色分组;②`shadow` 令牌改**柔和双层**
> 浅 `0 1px 2px .05 + 0 8px 24px .10`/深同构(小→大顺序),popup 与聊天页面板共用不割裂;
> ③类别徽标 chip 降级 **6px 小圆点 + 灰字**(点色同源 semantic 金/绿;popup 金标 ★ 是控件态
> 不动);④页脚键位**键帽化** `.pddcs-kbd`(与 popup HotkeyRow 的 kbd 同语言))
> v2.6.15 增补:候选切换**循环化**(第二十四轮,2026-09-16 用户反馈)—— 末条再按回绕到
> 首条(`moveSelection` 夹取改模回绕);**Shift+Tab 反向删除**(`panelNavKeys` 一并移除,
> 内容脚本直接用 `panelNavHotkey`);设置页「候选切换键」说明文字删除
> v2.6.14 增补:三处视觉修正(第二十三轮,2026-09-16 用户反馈)—— ①推荐面板**不透明**
> (背景 openPopup 内联双保险 + `opacity:1`,防平台页面样式干扰);②选中态**弃蓝改灰**:
> `accentBg` 令牌废弃,换 `selectedBg`/`selectedBar` 中性灰(浅色 rgba(0,0,0,.06)/.24,
> 深色 rgba(255,255,255,.10)/.32);③**细滚动条公共化** `ui/scrollbar.ts#thinScrollbarCss`
> (6px 悬浮显现,popup `.pddcs-scroll` 与聊天页 `.pddcs-popup` 同规格,8px→6px)
> v2.6.13 增补:面板导航键 **Tab/Shift+Tab**(第二十二轮,2026-09-16 用户反馈"↑↓ 与切换会话
> 冲突")—— ↑↓ 不再拦截、让位平台;新设置项「候选切换键」(`panelNavHotkey`,默认 Tab,
> **可录纯单键**,上一候选固定 = 同键 + Shift 取反,`pddUiLogic#panelNavKeys`);
> 选中态加强:新增 `accentBg` 令牌(accent 软底),选中行 = accent 软底 + 3px 左描边
> (原 btnHoverBg 底太淡难辨),`:hover` 同列防底色互盖
> v2.6.12 增补:快捷键面板**键盘导航**(第二十一轮,2026-09-16 用户指定)—— 面板打开即选中
> 第一条(accent 左描边 + 悬浮同款底色),↑↓ 移动(端点夹取不回绕,`pddUiLogic#moveSelection`),
> Enter 填充**选中项**(原固定第一条);鼠标悬浮同步选中(两套高亮共用一态);
> 仅快捷键路径持有选中态,点击「AI回复」面板保持纯点击交互不抢键盘
> v2.6.11 增补:推荐回复面板配额改 **3/3/3**(第二十轮,2026-09-16 用户指定)—— 标准回答至多 3 条
> (原"全部展示")、历史 top-3(原 2)、知识库 top-3(原 1);知识库检索阈值独立
> (`kbThreshold`,默认 0.4,原与金标准同走一档),设置页新增「知识库阈值(放宽)」滑杆
> v2.6.10 增补:金标控件降权重(第十九轮,治"按钮挤压内容区")—— 未设态改 **mini 幽灵钮**
> 「★ 设为标准」(controlH.inline 24px / caption 字号 / 细边 textMuted,悬浮 accent 描边,
> ≈74px,原 primary 黑钮 ≈100px);已设态改**琥珀 ★「标准回答」徽标**(semantic.golden 令牌,
> 与 Badge golden 同源),两态占宽一致无布局跳动;纯图标方案否决(两次"隐蔽入口"教训)
> v2.6.9 增补:记忆卡片操作重排 ——「设置标准回答」放回**每条回复行内**(单/多回复口径统一,
> 废止 v2.6.6 的"单回复移底部操作行");「删除」移至问题行**折叠钮左侧**并以垃圾桶图标展示
> (与折叠钮同取 controlH.inline 严格等高,悬浮染 errorBg;确认条改在问题行下方出现);
> 预置文件夹「未分类」更名「默认文件夹」(id 不变,存量由 ensurePresetFolders 启动时迁移)
> v2.6.8 增补:设置项标签统一加粗(semibold 600:Toggle/Slider 标签与快捷键行「快捷键」)、
> 快捷键行两行式重排 —— 行1 = 标签 + kbd 展示 + 「修改」钮推至行右(marginLeft:auto),
> kbd 显式取 controlH.form(26px)与「修改」钮严格等高(等高铁律);行2 = 说明/录入提示
> v2.6.7 增补:设置页控件重设计 —— 开关改「文字居左、开关居右」行式(36×20 轨道,滑块位移/按压拉伸
> 由 RESET_CSS 按 aria-checked/:active 驱动,选中 accent、未选中专用轨道令牌 switchTrack);
> 滑杆换自定义填充轨道(4px 圆轨按值 accent 硬切填充 + 14px 白圆描边拇指钮,悬浮/按住放大);
> 各项说明文案精简为一行口径(开/关行为式)
> v2.6.6 增补:「自动回复」开关语义修正(关 = 点击「AI回复」/快捷键都弹推荐回复面板,
> 移除原"单候选免面板静默直填"——用户反馈开关关就不该自动填)、
> 记忆卡片「设置标准回答」移到底部操作行(单回复卡片,紧挨「删除」右侧;多回复仍逐回复携带)、
> 文件夹「重命名/删除」图标钮常驻可见(悬浮+双击两代入口用户都找不到,不再依赖悬浮显隐)
> v2.6.5 增补:「新建」主钮抽为公共组件 `CreateBtn`(加号 + 文字,Btn primary 承载)——
> 知识库页「新建条目」与文件夹页「新建文件夹」统一同款,文件夹页原手搓 button 移除
> v2.6.4 增补:检索改走 SW 内存缓存(三源已嵌条目,写路径失效,见 retrievalCache.ts)、
> 记忆列表显式分页(底部「加载更早(已显示 X/Y 条)」,载完显示「已显示全部 Y 条」,不再静默截断 100 条)、
> 设置页新增「存储」卡(各表计数 + 浏览器存储用量 + 清空问答数据,危险操作二次确认)、
> 文件夹页遗留子夹「一键拍平」横幅(虚线边框条,金标准上移父夹后删除子夹)
> v2.6.3 增补:覆盖层跟随主题(聊天页面板/按钮读 `pddcs:theme` 实时重建,不再固定浅色)、
> 导航激活 2px 左指示条(非背景块)、Toggle 键盘可达(tabIndex + Enter/Space + 焦点环)、
> Btn 四种 variant 全部有悬浮底色(新增 `errorHoverBg` 令牌)、知识库手工条目去徽标(仅文档块保留「文档」)、
> Notice 成功提示 4s 自动消失(失败常驻)、记忆页「已设为标准回答」徽标持久化(库级 goldenId 回带)
> v2.6.2 增补:一致性三处对齐 —— ①「新建文件夹」升为主按钮(黑底白字,与页内主路径一致);
> ②知识库卡片次级操作(复制/编辑/停用/删除)收进行悬浮层(与文件夹页同款 `.pddcs-row-ops`,删除确认时常驻);
> ③危险按钮统一软底色(errorBg 底 + errorText 字,替换"纯 errorText 底 + 白字"的两处旧形态:确认删除行内钮、BtnMini danger)
> v2.6.1 增补:文件夹页仅保留一级文件夹(移除子文件夹创建入口,遗留子夹数据仍可展示/删除)、
> 「新建根文件夹」更名「新建文件夹」、设置页快捷键行说明文字独立换行(不再与控件挤压)、
> 记忆卡片折叠钮移至行尾(文字居左)、文件夹支持双击重命名
> v2.6 增补:按钮亮暗切换规范(暗色主按钮改中性深灰)、导航图标彻底去背景、文件夹树二次重设计(去引导线/统一缩进)、
> 推荐回复面板固定配额组成(标准回答全部 + 历史最近 2 + 知识库 1,快捷键面板同构;v2.6.11 改 3/3/3)
> v2.5 增补:暗色主按钮改 Figma 蓝底白字、导航栏极简化(只保留图标)、推荐回复面板(改名/去得分/类别保障/快捷键)
> v2.4 增补:文件夹页分区容器化重设计、顶部概览按页签分散、开关选中态改 accent、popup 宽 420
> v2.3 增补:标准回答「一问题多条(上限 3)」的展示与操作口径(候选/记忆/文件夹三处)
> v2.2 增补:控件等高铁律与 `controlH` 令牌、`Notice` 吸附顶部、聊天页按钮与气泡的真实外缘锚定
> 代码真源:`src/ui/design.ts`(几何与字型)+ `src/ui/theme.ts`(配色)
> 组件资产:`src/ui/components.tsx`(popup 复用)、`src/ui/scrollbar.ts`(细滚动条公共生成器,
> v2.6.14:popup 滚动容器与聊天页推荐面板同规格);聊天页覆盖层 CSS 由 `pdd-ai-button.ts` 从同一令牌导入插值

---

## 一、设计原则

1. **白面板 + 细边框**:层级靠 1px #E5E5E5 边框与明度差(#FFF / #FAFAFA / #EFEFEF),不用重描边、不堆卡片。
2. **黑白主色**:主操作黑底白字(#161616);Figma 蓝(#0D99FF)只用于焦点、品牌标与滑杆等状态,不做大色块。
3. **小圆角**:控件一律 4/6/8px,靠近桌面工具软件质感;圆形仅保留给开关、圆点与计数徽标。
4. **行悬浮操作**:列表行的次级操作(编辑/删除/迁移等)默认隐藏,悬浮或键盘聚焦时显现——静止界面只保留主路径。
5. **层级靠字重与字号**:13px 级正文 + 加粗标题 + 灰色辅助,小字号高密度。
6. **禁令**:页面代码不得硬编码字号 / 圆角 / 间距 / 颜色 / 字体,一律引用设计令牌
   (popup 走 ThemeTokens/React 内联;聊天页覆盖层在 CSS 模板里插值同一批令牌)。

## 二、配色(ThemeTokens,`ui/theme.ts`)

| 令牌 | 浅色 | 深色 | 用途 |
|------|------|------|------|
| `bg` | `#FFFFFF` | `#1E1F21` | 画布(popup 即白面板) |
| `bgSecondary` | `#FAFAFA` | `#1A1B1D` | 导航栏、行悬浮底色 |
| `bgCard` | `#FFFFFF` | `#2C2C2C` | 卡片底(靠边框成形) |
| `text` | `#161616` | `#E6E6E6` | 主文本 / 主按钮底 |
| `textMuted` | `#5C5C5C` | `#9B9DA2` | 次级文本 |
| `textTertiary` | `#8C8C8C` | `#6F7175` | 辅助/时间戳/图标默认色 |
| `border` / `borderLight` | `#E5E5E5` / `#EFEFEF` | `#3B3D40` / rgba(255,255,255,.06) | 分隔 |
| `accent` | `#0D99FF` | `#4CB3FF` | 焦点/品牌/**开关选中态**(Figma 蓝) |
| `btnPrimaryBg` | `#161616`(白字) | `#45484D`(白字) | 主按钮(见下方按钮亮暗规范) |
| `successText` | `#14AE5C` | `#0ACF83` | 成功提示 |
| `errorText` | `#F24822` | `#FF7262` | 错误/危险 |

**按钮亮暗切换规范**(2026-09-15 v2.6 定稿,参照用户示例图):两种主题**同构映射**,切换主题时按钮不允许跳色
(历史教训:近白主钮过亮 → Figma 蓝与中性色系脱节,最终定稿中性灰):

| variant | 浅色 | 深色 |
|---------|------|------|
| default | 面色底 `#FFF` + 边框 `#D4D4D4`,悬浮 `#FAFAFA` | 面色底 `#2C2C2C` + 边框 `#3B3D40`,悬浮 `#383B3D` |
| primary | 主色底白字 `#161616`,悬浮 `#333` | 面色系提亮一档 `#45484D` 白字,悬浮 `#53565B` |
| danger | `errorBg` 底 + `errorText` 字 | 同浅色(rgba 底自动适配) |

规则:default = 面色 + 边框,悬浮提亮一档;primary = 主色底白字,悬浮再提亮一档;
Figma 蓝只保留给焦点/开关/滑杆等**状态**,不做按钮大色块。

语义色(与主题无关,`design.ts#semantic`):标准回答徽标 amber(`#B8860B` on `#FDF6E3`)、知识库徽标绿(`#14AE5C` on `rgba(20,174,92,.09)`)。

## 三、字型

| 令牌 | 值 | 用途 |
|------|-----|------|
| `fontSize.caption` | 10.5 | 时间戳、来源、脚注、mini 钮 |
| `fontSize.secondary` | 11.5 | 回复正文、描述 |
| `fontSize.body` | 12.5 | 问题/条目标题、按钮(默认) |
| `fontSize.title` | 13.5 | 弹窗标题 |
| `fontSize.heading` | 15 | 页面标题 |
| `fontWeight` | 400 / 500 / 600 / 650 | 常规 / 中等 / 半粗 / 仅页面标题 |

字体栈(`design.ts#fontFamily`,工具软件同款):`"Segoe UI", "Microsoft YaHei", -apple-system, "PingFang SC", sans-serif`。
不联网加载字体;数字统一 `tabular-nums`。

## 四、几何

| 令牌 | 值 | 用途 |
|------|-----|------|
| `radius.sm / md / lg / xl` | **4 / 6 / 8 / 8** | 徽标与图标钮 / 按钮输入框 / 卡片 / popup 外框与浮层 |
| `radius.pill` | 9999 | 仅开关、圆点、圆形计数徽标 |
| `spacing.xs→xxl` | 4 / 6 / 8 / 10 / 12 / 16 | 4 的倍数栅格 |
| `size.popupWidth / popupHeight` | **420** / 560 | popup 固定外框(内容区滚动;v2.4 由 400 加宽,容纳文件夹树层级) |
| `size.railWidth / railBtn` | 52 / 36 | 图标导航栏 / 导航按钮 |
| `controlH.inline / form` | **24 / 26** | 行内小控件 / 表单与工具栏控件(见下) |
| `motion.fast / normal` | 0.12s / 0.15s ease | 悬停 / 开关过渡 |

**控件等高铁律**(2026-09-15 补):同一行内的输入框与按钮必须取同一档高度,
且用**显式 `height`** 而不是靠 `padding + line-height` 撑 —— 后者会随字号/行高漂移,
出现几像素的高低位错(实测:新建子文件夹输入框 35px vs「创建/取消」28px)。
- `controlH.inline = 24`:行悬浮图标钮、行内小按钮(填充/确认/取消)、迁移下拉。
- `controlH.form = 26`:`.pddcs-btn`、工具栏按钮、内联表单(新建/重命名)的输入框与按钮。
- 与按钮同排的输入框走 `components.tsx#controlStyle(tk, controlH.form)`。
- 聊天页覆盖层不享 popup 的全局 `box-sizing: border-box` 重置,注入样式须自带。


## 五、组件资产(`ui/components.tsx`)

| 组件 | 说明 |
|------|------|
| `Btn` | 直角按钮(6px),4 种 variant:default / primary(黑底白字) / danger / ghost;**四种 variant 全部有悬浮底色**(v2.6.3:primary 提亮一档、danger 加深一档 `errorHoverBg`、ghost/default 面色提亮一档) |
| `CreateBtn` | 工具栏「新建」主钮公共组件(v2.6.5):加号图标 + 文字,Btn primary 承载;知识库页「新建条目」与文件夹页「新建文件夹」共用,新增"新建类"入口一律走它,不再手搓 |
| `Card` | 卡片容器(白底 + 1px #E5E5E5 边框 + 8px 圆角),可选标题 |
| `Badge` | 徽标,3 种 tone:golden / knowledge / neutral;4px 小方标;知识库页手工条目**不挂徽标**(v2.6.3:本页即知识库,徽标只用于标记文档块) |
| `Notice` | 结果提示条(成功绿 / 错误红);**吸附在滚动区顶部**(`position: sticky; top: 0` + `bg` 底板),长列表下操作反馈不会被顶出视口;**成功提示 4s 自动消失**(v2.6.3,`onDismiss` 回调父级清 state;失败提示常驻供读错因) |
| `EmptyState` | 空状态(居中、两行文案) |
| `SearchInput` | 带放大镜的搜索输入框 |
| `Toggle` | 拨杆开关,**行式布局:文字居左、开关居右**(v2.6.7 用户要求;36×20 轨道 + 16 滑块)。标签 semibold 加粗(v2.6.8)。选中态 accent(悬浮 accentHover),未选中走专用令牌 `switchTrack`(悬浮 `switchTrackHover`,比 inputBorder 深一档)。**滑块位移与按压拉伸(顺拖动方向 +2px)全由 popup RESET_CSS 按 `aria-checked`/`:active` 驱动**,React 只声明结构(knob 无内联 left,内联会压过 :active);**键盘可达**(v2.6.3:`tabIndex=0` + Enter/Space 切换 + focus-visible accent 焦点环) |
| `Slider` | 数值滑杆(v2.6.7 重设计):4px 圆轨**按值填充**(accent 到当前值硬切,渐变由组件按 (value-min)/(max-min) 内联注入)+ 14px 白圆 accent 描边拇指钮(RESET_CSS `.pddcs-slider`,悬浮放大 1.12、按住 1.22);标签 semibold 加粗(v2.6.8);数值 tabular-nums 右对齐 |
| `SectionLabel` | 小节标签 |
| `inputStyle` | 表单元素统一样式原语(独立成行的输入框/文本域) |
| `controlStyle` | 与按钮同排的输入框样式原语(显式高度,保证等高) |
| `.pddcs-row-ops` | 行悬浮操作容器(全局 CSS):默认透明,`:hover` / `:focus-within` 显现 |

图标:统一走 `ui/icons.tsx`(lucide-react 封装,24px 画布 / 2px 描边 / 圆角线帽);禁止使用 emoji 充当图标。
例外:聊天页「AI回复」按钮为纯文字小控件,不带图标(2026-09-15 用户要求)。

## 六、聊天页覆盖层(`contents/pdd-ai-button.ts`)

CSS 由纯函数 `ui/overlay-css.ts#buildOverlayCss(tk)` 按主题令牌生成
(**颜色/字体/圆角/字号全部从 `ui/design` + `ui/theme` 插值**,与 popup 同源)。
**跟随 popup 主题设置**(v2.6.3,替换原"固定浅色"):启动读 `chrome.storage` 的 `pddcs:theme`,
`storage.onChanged` 实时整体重建 `<style>`;toast 保持深底白字不随主题切换。
存储值容错解析(`parseThemeMode`:非法/缺失回退浅色)。

- `.pddcs-ai-btn`:AI回复按钮 —— 与 popup 的 `.pddcs-btn` **同档工具风控件**
  (26px 高 / 6px 圆角 / 12.5px 字号 / 白底细描边 / 悬浮浅灰;纯文字无图标,2026-09-15 用户定)。
  `box-sizing: border-box` 显式声明;位置为气泡真实右缘外 12px。
- `.pddcs-popup`:候选弹窗(v2.6.18 重设计:**三段式浮层壳** —— 头/页脚常驻,滚动只发生在
  中段 `.pddcs-popup-body`(6px 细轨挂此),popup 本体 `overflow:hidden` 负责裁圆角;
  入场 160ms 淡入上移;v2.6.19 圆角 8→12px `radius.xxl`)。柔和双层浮层阴影;
  候选行为**内缩圆角软行**(悬浮 = 选中同一软中性灰填充;**操作钮悬浮/选中才显**),
  类别徽标**软底色 chip**(v2.6.19:标准回答=琥珀软底金字,知识库=绿软底绿字,历史=中性灰),
  「同内容×n」行右下角悬浮才显,页脚键位**键帽化** `.pddcs-kbd`。
- `.pddcs-toast`:近黑(#161616 @ .92)轻提示,6px 圆角。

**气泡锚点算法**(2026-09-15 修复「按钮压住气泡」):真机结构
`li.onemsg > .buyer-item > div[currentuid] > .msg-content > p.msg-content-box`,
气泡底色与内边距在上层容器上,`<p>` 的 rect 右缘短掉气泡 padding(实测约 10px)——
按 `<p>` 右缘 +12px 定位,视觉间距只剩约 1px。
现自文本块向上吸收「有不透明背景色 且 宽度贴近文本块(容差 48px)」的最外层祖先作锚,
宽度守卫排除整行容器的底色;兜底取文本块直属容器。算法落在
`src/pdd/bubble-anchor.ts`(含单测),内容脚本只做 WeakMap 缓存。

## 七、文件夹模块布局(2026-09-15 重构)

工具风单栏分区树,参照 pddddd 知识库 doc-list 与导航折叠模式:

- **工具栏**:右上角「新建文件夹」直角小钮;新建时原位变内联表单(Enter 提交 / Esc 取消)。
- **根文件夹分区**:可折叠分区头(chevron 旋转 + 文件夹图标 + 名称 + 圆形计数徽标),分区间以细线分隔;「默认文件夹」置底且不可改名/删除(v2.6.9 由「未分类」改名,id 不变)。
- **子文件夹**:左侧 1px 引导线缩进,头部字重降为 medium。
- **标准回答行**:分隔线行(非卡片)。问题加粗单行、回复灰色两行、向量状态为行内小字;
  操作分两层——「填充」黑色主钮常驻,复制/编辑/迁移/删除为悬浮显现的 24px 图标钮(删除悬浮红)。
- **一问题多条**:同一问题的标准回答最多 3 条,行内按**设置时间倒序**;
  同问题 >1 条时,问题行右侧标 `同问题 n 条`,达上限追加 ` · 已满`(提示先取消一条)。
- **内联交互**:重命名在分区头原位表单化;删除走内联确认行(常驻可见,不依赖悬浮)。
  内联表单的输入框与「创建/保存/取消」严格等高(`controlH.form = 26`)。
- **重命名入口(v2.6.6 定稿)**:「重命名/删除」图标钮**常驻可见**(不再悬浮显隐)——
  v2.6.1 给过"悬浮图标钮 + 双击文件夹名"两代入口,用户仍找不到、再次提出"支持修改名称";
  双击改名保留作为快捷方式。「默认文件夹」不可改名。

**记忆列表卡片**(同一工具语言):卡片头**问题与元信息居左**,行尾两枚 24px 图标钮 ——
**垃圾桶(删除)在左、chevron 折叠钮在右**(v2.6.9 用户要求;同取 `controlH.inline` 严格等高,
垃圾桶悬浮染 errorBg/errorText;折叠钮 chevron 折叠时旋转 -90°,带 `aria-expanded`;整行仍可点击折叠);
展开区与问题文本同左基线(不再为图标钮留缩进);
「设为标准」控件(v2.6.10 定稿):**每条回复行内各自携带**(单/多回复口径统一;
v2.6.6 曾移底部操作行、v2.6.9 放回行内用 primary 钮,本轮按"挤压内容区"反馈降为 mini 幽灵钮);
未设态「★ 设为标准」(24px/caption/细边,悬浮 accent),已设态琥珀「★ 标准回答」徽标
(`semantic.golden` 令牌);点击原位翻转,占宽一致;
删除确认:点垃圾桶图标后**问题行下方**出现确认条「删除该问答及其全部回复?」+ 确认(danger)/取消(ghost);
已提升的回复原位显示绿色对勾「已设为标准回答」——
**徽标持久化**(v2.6.3):`GET_MEMORY_LIST` 按 `goldens.sourceReplyId` 回带 `goldenId`,
前端从数据派生,重开 popup、取消后都如实反映库内状态(原会话级 state 重开即丢)。

**分页入口**(v2.6.4,PM2):列表默认取最近 100 条,底部居中 ghost 钮
「加载更早(已显示 X/Y 条)」追加下一页(offset = 已加载条数,X 为已加载数、Y 为排除自检后的全量总数,
与头部统计同口径);全部载入后按钮替换为 caption「已显示全部 Y 条」。
原先无提示截断 100 条,与头部全量统计自相矛盾。

**遗留子夹拍平横幅**(v2.6.4,PM7):存在 `parentId !== null` 的存量子夹时,列表顶部出现
虚线边框条(1px dashed border + bgSecondary 底)——左侧 caption「检测到 n 个遗留子文件夹(新版仅支持一级文件夹)」,
右侧「一键拍平」描边小钮;点击原位变 `confirmRow` 二次确认(拍平 = 子夹金标准上移父夹,父夹失联归「默认文件夹」,随后删除子夹)。
拍平成功后横幅自然消失(无子夹即不渲染)。

## 七-A、顶部概览按页签分散(v2.4)

页签标题下的一行小字**只显示与本页相关的计数**(原先四个计数全堆在顶部,与当前页无关):

| 页签 | 概览行 |
|------|--------|
| 记忆 | `问答 n · 回复 m` |
| 文件夹 | `文件夹 k · 标准回答 g` |
| 知识库 | `知识 n` |
| 设置 | **不展示**(行高保留,避免切页签跳动;全量计数见页内「存储」卡,v2.6.4) |

## 七-D、设置页「存储」卡(v2.6.4,PM6a)

位于「导入与导出」与「关于」之间,三段式:

1. **计数行**:问答 / 回复 / 标准回答 / 知识 / 文件夹 五个计数(caption、tabular-nums,可换行);
   追加浏览器存储用量 `已用 x MB(配额约 y GB)`(`navigator.storage.estimate()`,无该 API 时此段隐藏)。
2. **操作行**:「清空问答数据」danger 钮(软底色规范同 v2.6.2)→ 点击原位变
   警示文案 + 「确认清空」/「取消」。只清问答与回复(含自检数据),**标准回答/知识库/文件夹保留**;不可撤销。
3. **说明行**:caption「问答记录与回复按保留期自动清理;此操作立即清空全部问答数据(含自检数据),不可撤销。」

操作成功/失败走页顶 `Notice`(成功 4s 自动消失),成功后刷新计数与用量。

## 七-B、文件夹页分区容器化(v2.4 引入,v2.6 二次重设计)

用户反馈"文件夹的表示混乱":v2.4 用"分区容器 + 引导线"重排后,实机评审发现
引导线(`borderLeft`)与容器边框、行分隔线叠在一起仍然显乱。v2.6 定稿:

- **根文件夹 = 一张分区容器**:1px 边框 + 8px 圆角 + `overflow:hidden`,头部为标题栏
  (常驻 `bgSecondary` 底,高 34,展开时底边 `borderLight`);「默认文件夹」置底用**虚线边框**。
- **层级只靠三件事表达**:统一缩进节奏(`padding-left = 12 + 16×深度`,分区头/子夹行/
  标准回答行/内联表单/空态占位全部同基线)、字重字号(根夹 semibold/body,子夹 medium/secondary)、
  根夹标题栏底色。**不使用引导线与嵌套边框**(v2.6 移除)。
- **子夹与根夹同构**:同一行结构(chevron + 文件夹图标 + 名称 + 计数徽标 + 悬浮操作),
  高 30、透明底、悬浮 `bgSecondary`;仍排在父夹标准回答**之前**(资源管理器直觉)。
- **仅一级文件夹(v2.6.1 定稿)**:子文件夹创建入口移除,「新建根文件夹」更名**「新建文件夹」**;
  历史遗留的子夹数据仍照常展示(同构行)与删除,但不支持再新建 —— 扁平结构比两层树更符合本工具的使用密度。
- **标准回答行**:分隔线行(非卡片),与所在分区头同基线左对齐;「填充」主钮常驻,
  复制/编辑/迁移/删除悬浮显现。
- **空文件夹**:灰字"暂无标准回答"占位(与分区同基线缩进)。
- **工具栏**:「+ 新建文件夹」左对齐,内联新建时原位变表单。

## 七-C、聊天页推荐回复面板(v2.5 引入,v2.6 固定配额组成)

原「AI 候选」面板更名与升级:

- **命名**:面板标题「推荐回复(n)」;得分数字不再展示(对客服无决策价值),同内容折叠数保留。
- **固定配额组成**(2026-09-16 v2.6.11 用户指定,三类各 top-3;替代 2026-09-15 的
  "标准全量 + 历史 2 + 知识库 1"):
  **标准回答至多 3 条**(按设置时间倒序)+ **历史取最近 3 条**(按回复时间倒序,与相关度无关)
  + **知识库取相关度最高 3 条**。配额在折叠后胜者池上选取
  (`retrieval.ts#PANEL_QUOTA`),某类别候选存在就必然占位,无需事后补位;
  「标准回答优先」开关只影响排序,不影响配额。点击「AI回复」与快捷键唤起的面板**完全同构**。
- **知识库阈值独立**(v2.6.11):知识库检索门槛由"与金标准同走一档"改为独立
  `kbThreshold`(默认 0.4,同档放宽),设置页「知识库阈值(放宽)」滑杆(0.2~0.8,步进 0.05)可调。
- **键盘导航**(v2.6.12 引入,v2.6.13 改键,v2.6.14 改灰,v2.6.15 循环化,v2.6.18 改纯软灰填充,
  仅快捷键唤起的面板):打开即选中第一条
  (高亮 = **中性灰软底 `selectedBg` 圆角填充**,v2.6.18 起**去 3px 灰左描边**——`selectedBar`
  令牌随行内缩圆角化一并移除,悬浮与选中共用同一软灰填充;用户指定弃用 accent 蓝);
  **Tab 单键循环**移动选中项(键 = 设置「候选切换键」,可自定义含纯单键;末条再按
  **回绕到首条**,v2.6.15 用户指定;Shift+Tab 反向已删,`panelNavKeys` 一并移除,
  `pddUiLogic#moveSelection`),选中行滚进可视区(v2.6.18 三段式后滚动容器是
  `.pddcs-popup-body`,头/脚已移出滚动视口,头部实高补偿随之取消,`headH=0`;
  保留**选中首条直接 scrollTop=0** 的回绕回顶语义);
  `Enter` 填充
  **选中项**(原固定第一条),脚注按当前设置动态显示「Tab 切换候选(循环),Enter 填充;发送请手动点击」。
  `↑`/`↓` **不拦截**(2026-09-16 用户反馈与平台"↑↓ 切换会话"冲突,按键让位给页面)。
  鼠标悬浮把选中态一并带过去 —— 两套高亮共用一态,不出现底色与描边打架;
  点击「AI回复」打开的面板不进入键盘模式(不拦导航/Enter,保持页面原有键位)。
- **不溢出**:面板挂载后量 `getBoundingClientRect().height` 向上取整,垂直方向整体夹在视口内
  (亚像素高度要用 rect 量,`offsetHeight` 是取整后的整数会差 0.1px 压线)。
- **快捷键**(默认 `Ctrl + Enter`,设置中可录入自定义组合,需带 Ctrl/Alt/Shift):
  - 「自动回复」关:检索**用户最新消息** → 弹推荐回复面板(与点击「AI回复」同构)→ 再按 `Enter` 填充第一条;
  - 「自动回复」开:检索 → 直接填充第一条;
  - 开关语义(v2.6.6 修正):开/关对**点击「AI回复」与快捷键两条路径同等生效**。
    原实现"关 + 单候选 → 免面板静默直填"与开关语义冲突,已移除——用户反馈:
    关闭就是不希望自动填充,哪怕只有一条候选,也应弹面板让人确认。
  - 面板由快捷键唤起时脚注提示「按 Enter 填充第一条」;Esc/点外部关闭即解除。
  - 快捷键在设置页展示(`kbd` 样式)+「修改」按钮进入录入态。
  - 行布局(v2.6.8 重排):第一行 = 标签(semibold)+ `kbd` + 「修改」钮推至行右
    (`marginLeft:auto`);`kbd` 显式取 `controlH.form` 与「修改」钮严格等高(等高铁律)。
    第二行 = 说明或录入提示,独立一行 —— popup 宽度有限,四段挤一行会把「快捷键」标签压成竖排。

## 八、文案规范

- 概念命名(2026-09-15 起):「金标准」一律称 **标准回答**;聊天页面板称 **推荐回复**(v2.5,原"AI 候选");
  提升操作按钮为 **设置标准回答**,
  已在标准回答状态时的反向操作为 **取消标准回答**(只解除身份,不动历史记录)。
- 上限提示(2026-09-15):「该问题已有 3 条标准回答,请先取消一条」;成功提示带额度 `已设为标准回答(2/3)`。
- 操作按钮:两字优先(填充 / 复制 / 编辑 / 删除 / 确认 / 取消 / 创建 / 保存 / 启用 / 停用);
  例外:设置标准回答(用户指定全称)、新建文件夹(入口钮)。
- 空状态:「暂无 ××」+ 一句引导。
- 结果提示:动宾结构直述结果;固定表述「发送由人工完成」「后台将自动向量化」。
- 技术细节放 tooltip / title,不挤占界面文案。

## 九、popup 全局样式(注入于 `popup/index.tsx`)

- `.pddcs-btn`:直角基础样式(6px 圆角、边框、过渡);**高度固定 `controlH.form`(26px)**,
  保证与同排输入框/工具栏按钮等高。
- `.pddcs-input`:输入框基础样式(6px 圆角,focus 由内联 border 提亮)。
- `.pddcs-scroll`:悬浮才出现的细滚动条。
- `.pddcs-rail-btn`:导航图标按钮 —— **任何状态下都只有图标本身,无背景块**
  (v2.6 用户要求,含悬浮态;2026-09-15 v2.5 起默认态已去背景块),
  激活态用图标颜色(`text` vs `textMuted`)、描边粗细(2.4 vs 1.8)与 **2px 左侧短指示条**
  (v2.6.3:贴导航栏左缘、高 14、`tk.text` 色;原激活态几乎不可辨,又不接受背景块)表达;
  导航栏顶部**不放品牌标**。
- `.pddcs-switch`:Toggle 开关的键盘焦点环 —— `.pddcs-switch:focus-visible` 画 2px accent
  外描边(v2.6.3;仅键盘聚焦显现,鼠标点击不出环)。
- `.pddcs-row(-ops)`:行悬浮操作显现规则(见 §五)。
- **令牌 → CSS 变量桥**:静态 CSS(RESET_CSS)读不到 React 令牌,凡随主题变化的静态规则一律走
  CSS 变量:`--pddcs-scroll-thumb`(滚动条滑块)、`--pddcs-accent`(焦点环)。
  注意**内联样式优先级高于类选择器**:不要给依赖 `:hover` 的元素内联写死对应属性,
  否则 `:hover` 永远不生效。
- 外框:`overflow:hidden; border-radius:8px`,body 背景透明。
- `* { box-sizing: border-box }`:popup 全局重置,表单控件显式高度才能生效。
