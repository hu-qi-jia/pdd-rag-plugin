# 拼多多客服快捷回复工具 — 工作状态记录

> 更新时间:2026-09-16(第二十二轮:面板导航改 Tab/Shift+Tab + 自定义键 + 选中态加强;单测 256/256)
> 仓库:`E:\个人项目\拼多多客服检索工具\personal-ai-memory`(自 personal-ai-memory 改造)
> 远程:`https://github.com/hu-qi-jia/pdd-rag-plugin.git`(2026-09-15 起;原 `hu-qi-jia/-.git`)
> 设计总纲:`E:\个人项目\拼多多客服检索工具\拼多多客服快捷回复工具-设计文档.md`

---

## 〇-V、2026-09-16 第二十二轮:面板导航键 Tab 化 + 自定义 + 选中态加强(单测 256/256,tsc 0 错,build 过,verify-ai-popup 18/18 · verify-ui 24/24 · verify-kb 19/19)

用户:「↑和↓快捷键和切换会话冲突了,修改为tab键向下切换吧。tab键也要放在设置中,可以自定义。选中状态再明显一点」。

| 项 | 内容 |
| --- | --- |
| **↑↓ 让位** | 移除第二十一轮的 ArrowUp/ArrowDown 拦截分支(原捕获态 preventDefault 吃掉了平台的"↑↓ 切换会话");↑↓ 按键完全放行给页面。e2e 新增回归锁:「dispatch ArrowDown → 选中不动」 |
| **Tab/Shift+Tab 导航** | 捕获态改按 `panelNavKeys(hotkeySettings.panelNavHotkey)` 匹配:next = 配置键(默认 **Tab**),prev = **同键 + Shift 取反**(表单导航同款惯例,纯逻辑 `panelNavKeys` 单测覆盖);夹取/Enter 填充选中项/悬浮同步选中逻辑不变;脚注按当前设置动态显示「Tab 切换候选,Enter 填充…」 |
| **候选切换键设置** | 新设置项 `panelNavHotkey`(`HotkeyConfig`,默认 `{key:'Tab'}` 无修饰键):PddSettings + `DEFAULT_PANEL_NAV_HOTKEY` + clampSettings(clampHotkey 增 fallbackKey 参,缺省回退 Tab 而非 Enter);设置页 HotkeyRow 参数化(label/desc/**allowPlainKey**),新增「候选切换键」行 —— 录入放开纯单键(唤起键仍要求带修饰键),提示文案「可用单键(如 Tab)」 |
| **选中态加强** | 新设计令牌 `accentBg`(accent 软底:浅色 rgba(13,153,255,.10)/深色 rgba(76,179,255,.16),沿用 successBg 的 rgba 软底模式);`.pddcs-cand-selected` 由 btnHoverBg 底 + 2px 描边 → **accentBg 底 + 3px accent 左描边**,并与 `:hover` 同列声明(同级 specificity 下 hover 会盖掉选中底色);亮暗主题都明显可辨 |

验收证据(代码级):单测 **256/256**(+2 `panelNavKeys`);`tsc --noEmit` 0 错;`plasmo build` 过;e2e `verify-ai-popup` **18/18**(脚注/Tab 逐条/夹取/Shift+Tab 回退/↑↓ 不拦截/Enter 填选中)、`verify-ui` **24/24**(候选切换键行 + 双「修改」钮 + 行内几何改按行取)、`verify-kb` **19/19**。
DESIGN.md 升 v2.6.13:§七-C 键盘导航条目改写 + accentBg 令牌。

**第二十二轮用户真机验收清单**:
1. 聊天页有多个会话时按 ↑↓:平台正常**切换会话**(不再被面板吃掉);
2. 按 `Ctrl+Enter` 弹面板 → `Tab` 逐条下移、`Shift+Tab` 回退,选中行**蓝色软底 + 左侧 3px 蓝条**(亮暗主题都醒目);
3. 设置页出现第二行快捷键「**候选切换键**」(kbd 显示 `Tab`),点「修改」可直接录单键(如 `Q`);
4. 选中第二条后 `Enter`:填入高亮那条,发送仍需手动点击。

---

## 〇-U、2026-09-16 第二十一轮:快捷键面板键盘导航(单测 254/254,tsc 0 错,build 过,verify-ai-popup 17/17 · verify-ui 23/23 · verify-kb 19/19)

用户:「当用户按快捷键弹出推荐回复窗口时,我想增加选择选中效果。用户可以按键盘↑和↓键进行选择和发送(未开启自动回复)。完成后提交并推送」。

| 项 | 内容 |
| --- | --- |
| **键盘导航** | `armedPanel` 扩为 `{items, selected}`:`openPopup` 增 `opts.keyboard`(仅快捷键路径传),打开即选中第一条;捕获态 keydown 拦截 `↑`/`↓` → `moveSelection`(纯逻辑,端点**夹取不回绕**)+ `scrollIntoView(nearest)`;`Enter` 填充**选中项**(原固定第一条),toast 改类别口径「已填充:标准回答/知识库/历史回忆 · 请手动发送」;脚注改「↑↓ 选择,Enter 填充;发送请手动点击」 |
| **选中态样式** | `overlayTheme.ts` 增 `.pddcs-cand-selected`(accent `inset 2px` 左描边 + `btnHoverBg` 底);**鼠标悬浮同步写同一选中态**(mouseenter)—— 两套高亮共用一态,不出现 hover 底色与选中描边打架;点击「AI回复」打开的面板不进键盘模式,不拦 ↑↓/Enter(保持页面原有键位) |
| **TDD** | `pddUiLogic.test.ts` RED→GREEN:`moveSelection` 3 断言(逐条移动/端点夹取/空表安全),10/10;全套 **254/254**(+3);`verify-ai-popup` 扩 5 检查(初始选中第一条/↓ 逐条/末尾夹取/↑ 回退/**Enter 填充的是选中项非第一条**),17/17 |

验收证据(代码级):单测 **254/254**(+3);`tsc --noEmit` 0 错;`plasmo build` 过;e2e `verify-ai-popup` **17/17**(+4)、`verify-ui` **23/23**、`verify-kb` **19/19**。
DESIGN.md 升 v2.6.12:§七-C 新增键盘导航条目。

**第二十一轮用户真机验收清单**:
1. 聊天页按 `Ctrl+Enter`:推荐面板弹出,**第一条带蓝色左描边高亮**;
2. 按 `↓`/`↑`:高亮随键移动,到首/末停住不回绕;鼠标划过候选,高亮跟随;
3. 选中第二条后按 `Enter`:输入框填入的是**高亮那条**,面板关闭,提示「请手动发送」;
4. 点击「AI回复」打开的面板:无键盘高亮(点击填充,行为不变)。

---

## 〇-T、2026-09-16 第二十轮:推荐面板配额 3/3/3 + 知识库独立阈值(单测 251/251,tsc 0 错,build 过,verify-ui 23/23 · verify-ai-popup 13/13 · verify-kb 19/19)

用户:「那更新一下推荐回复的面板展示逻辑:标准回答:有则展示,至多展示三条 / 历史:展示top-k=3,需符合设置中的置信度 / 知识库:展示top-k=3,在设置中也增加置信度滑动条,需符合设置中的置信度。完成后提交并推送」。

背景:第十九轮诊断确认知识库检索链路本身通畅(探针 score 0.908),用户感知识库"不出条目"的直接原因是面板配额只给知识库 1 个位;本轮按用户口径重构配额并给知识库独立阈值。

| 项 | 内容 |
| --- | --- |
| **配额 3/3/3** | `PANEL_QUOTA` `{history:2, knowledge:1}` → `{golden:3, history:3, knowledge:3}`:标准回答由"全部展示"改为**至多 3 条**(仍按设置时间倒序,`goldenPicked.slice(0,3)`);历史 top-3(回复时间倒序);知识库 top-3(相关度最高)。配额仍在折叠后胜者池上选取,类别占位语义不变;推荐回复与快捷键面板同构不受影响 |
| **知识库独立阈值** | `rankCandidates` thresholds 增 `knowledge` 档,门控三分支(history→simThreshold / knowledge→kbThreshold / 其余→goldenThreshold),知识库不再蹭金标准阈值。新设置项 `kbThreshold`(默认 0.4,与原行为同档,存量迁移无感):`PddSettings` + `DEFAULT_SETTINGS`、`clampSettings`、`search.ts` 阈值接线、`index.ts` GET_STATS 兜底字面量、设置页「知识库阈值(放宽)」滑杆(0.2~0.8,步进 0.05,置于金标准阈值之下) |
| **TDD** | `retrieval.test.ts` RED→GREEN:配额断言全面改 3/3/3 口径(标准 5→3 截断、历史 4→3 截最老、知识库按相关度),新增「知识库阈值独立于金标准」门控测试(cosine 0.45 过 golden 0.4 被 knowledge 0.5 滤),32/32;全套 **251/251** |

验收证据(代码级,无截图):单测 **251/251**(+2);`tsc --noEmit` 0 错;`plasmo build` 过;e2e `verify-ui` **23/23**、`verify-ai-popup` **13/13**、`verify-kb` **19/19**。
DESIGN.md 升 v2.6.11:§七-C 配额组成改写 + 新增知识库阈值条目;设计文档 §6.2/设置表同步。

**第二十轮用户真机验收清单**:
1. 打开设置页:检索与填充卡出现第四根滑杆**「知识库阈值(放宽)」**(默认 0.40);
2. 触发 AI 回复:推荐面板中标准回答至多 3 条、历史最多最近 3 条、知识库最多相关度最高 3 条(原先各 2/1);
3. 把知识库阈值拉高到 0.8 后再触发:知识库条目应按新阈值过滤(低于 0.8 不出)。

---

## 〇-S、2026-09-16 第十九轮:金标控件降权重(单测 249/249,tsc 0 错,build 过,verify-ui 23/23;按用户要求本轮不截图、仅代码验收)

用户:「设置标准回答的按钮宽度太大了,有点挤压内容区域的位置,你有什么好的建议吗,以设计师的身份来看。验收时不用截图了,只检查代码即可」。

| 项 | 内容 |
| --- | --- |
| **设计决策** | 病因:Btn primary(黑底白字 body 字号 ≈100px)逐回复重复,视觉权重压过内容区。三案对比:纯星标图标钮(省 ~86px,但本项目两次踩"隐蔽入口不可发现"坑,否决)/ 收进悬浮 ops(同因否决)/ **mini 幽灵钮(采纳)**。落地:未设态「★ 设为标准」(controlH.inline 24px、caption 10.5px、细边 inputBorder + textMuted,悬浮 accent 描边,≈74px 省 ~26%);已设态琥珀「★ 标准回答」徽标(semantic.golden/goldenBg,与全局 Badge golden 同源,替换原绿色长文案「已设为标准回答」);两态占宽一致,点击翻转无布局跳动;disabled「设置中…」保留 |
| **TDD** | `memoryGoldenBtnPlacement.test.tsx` +1 尺寸断言(height 24px / fontSize 10.5px,先 RED)、文案断言随新名更新(设为标准/标准回答),6/6 |
| **验收脚本** | verify-ui 金标步进改新定位器(hasText '设为标准' / 已设徽标按 title 命中),23/23 不变 |

验收证据(仅代码,无截图):单测 **249/249**(+1);`tsc --noEmit` 0 错;`plasmo build` 过;`verify-ui` **23/23**。
DESIGN.md 升 v2.6.10:头部版本行 + §七「设为标准」控件条目改写。

**第十九轮用户真机验收清单**:
1. 记忆页回复行内按钮变**小号幽灵钮「☆ 设为标准」**(灰字细边),内容区明显变宽;悬浮时描边与文字变蓝;
2. 点击后原位变**琥珀色「★ 标准回答」小徽标**,按钮与徽标宽度接近,无跳动;
3. 文件夹页/聊天页推荐面板中的同名动作不变(仅记忆页行内控件改版)。

---

## 〇-R、2026-09-16 第十八轮:记忆卡片操作重排 + 默认文件夹更名(单测 248/248,tsc 0 错,build 过,verify-ui 23/23,亮暗截图评审)

用户:「将记忆页中的设置标准回答放回在每个回答条目的后方 / 将删除按钮移动至折叠按钮左侧,并以图标展示 / 文件夹页中的默认文件夹,目前名称叫未分类,请修改为默认文件夹。完成后提交」。

| 项 | 内容 |
| --- | --- |
| **「设置标准回答」放回回答条目后方** | 废止第十六轮口径(v2.6.6 曾把单回复卡片的按钮移到底部操作行):`goldenControl` 现在**每条回复行内各自携带**,单/多回复口径统一,不再按 `replies.length` 分流;底部操作行整体移除 |
| **「删除」图标化并左置折叠钮** | 问题行行尾改为两枚 24px 图标钮:**垃圾桶(删除)在左、chevron 折叠钮在右**,同取 `controlH.inline` 严格等高(等高铁律);垃圾桶悬浮染 `errorBg`/`errorText` 危险暗示;原底部「删除」文字按钮消失。**确认条改挂问题行下方**(点图标后原位出现「删除该问答及其全部回复?」+ 确认/取消),二次确认语义保留 |
| **默认文件夹更名** | `UNCATEGORIZED_FOLDER_NAME` 值「未分类」→「默认文件夹」(**id `uncategorized` 不变**,识别一律按 id,FoldersTab/panelLogic 均无按名匹配)。**存量迁移**:`ensurePresetFolders()`(SW 启动即跑)发现预置行名 ≠ 新名时幂等改名 —— 该夹 UI 禁止改名,旧名只可能来自旧版本预置,覆盖安全;新装直接以新名创建。FoldersTab 三处用户可见文案(删除文件夹 toast/tooltip/确认条)同步 |
| **TDD** | `memoryGoldenBtnPlacement.test.tsx` 重写(3 条金标位置新口径 + 2 条删除入口:图标钮在折叠钮左侧同高/确认条出现-取消退出-确认调 DELETE_QA);`db-pdd.test.ts` 预置名断言更新 + 新增存量迁移测试(旧名"未分类"→ 启动改名);folderRename/panelLogic/panel/transfer 测试夹具与描述同步改名 |

验收证据:单测 **248/248**(28 文件,+3);`tsc --noEmit` 0 错;`plasmo build` 过;`verify-ui` **23/23**(18→23:+5 —— 删除图标钮在折叠钮左侧同高 24/24px、纯文字删除钮命中 0、点图标出确认条、取消可退出、文件夹页「默认文件夹」且全文无「未分类」);
截图人工评审:`logs/ui-0915-shot-memory-light.png`(每条回复后方「设置标准回答」、行尾垃圾桶+chevron)、`-folders-light.png`、`-memory/dark` 等亮暗全套。
DESIGN.md 升 v2.6.9:头部版本行、§七记忆列表卡片条目重写(操作重排+确认条新位置)、文件夹页四处「未分类」改「默认文件夹」;设计总纲预置夹名同步(4 处)。

**第十八轮用户真机验收清单**:
1. 记忆页:**每条回复后方**都有「设置标准回答」按钮(单回复卡片也回到回复行内);点击后原位变「已设为标准回答」;
2. 每张卡片问题行**右上角:垃圾桶图标在折叠箭头左侧**,两者同高;垃圾桶悬浮变红底红字;
3. 点垃圾桶 → 问题行下方出现「删除该问答及其全部回复?」确认条,取消可退出、确认后删除;底部不再有「删除」文字按钮;
4. 文件夹页:预置夹显示为**「默认文件夹」**(旧数据升级后自动改名);删除普通文件夹时提示文案同步为「移入『默认文件夹』」。

---

## 〇-Q、2026-09-15 第十七轮:设置项标签加粗 + 快捷键行重排(单测 245/245,tsc 0 错,build 过,verify-ui 18/18,亮暗截图评审)

用户:「自动回复、快捷键、标准回答优先...的字体加粗。快捷键的快捷键展示(Ctrl + Enter)高度与修改按钮高度一致,"修改"按钮移动至右侧。完成后提交」。

| 项 | 内容 |
| --- | --- |
| **标签统一加粗** | `Toggle`/`Slider` 组件的标签 span 与快捷键行「快捷键」标签统一 `fontWeight.semibold`(600,design.ts 令牌);三处共用同一令牌,后续调整一处生效 |
| **快捷键行两行式重排** | `HotkeyRow` 重构:行1 = 标签 + `kbd` 展示 + 「修改」钮(`marginLeft:auto` 推至行右);行2 = 说明或录入提示(互斥渲染)。`kbd` 显式取 `controlH.form`(26px)+ inline-flex 垂直居中 + `padding: 0 10px`,与「修改」钮(.pddcs-btn 同档 26px)**严格等高**(等高铁律,不再靠字体行高撑) |
| **验收脚本扩两项** | `verify-ui-2026-09-15.mjs` 设置页段新增几何断言:① kbd 与「修改」钮 `getBoundingClientRect().height` 相等;② 「修改」钮 left > kbd right(确在行右)。16→18 项 |
| **TDD** | `toggle.test.tsx` / `slider.test.tsx` 各加 1 条标签加粗断言(取文本匹配的 span,断言 `style.fontWeight === '600'`);243→245 |

验收证据:单测 **245/245**(28 文件,+2);`tsc --noEmit` 0 错;`plasmo build` 过;`verify-ui` **18/18**(几何实拍 kbd 26px = 修改钮 26px,修改钮 kbd.right=218 → btn.left=323);
截图人工评审:`logs/ui-0915-shot-settings-light.png` / `-dark.png`(00:02 新图,标签加粗 + 快捷键行重排两主题确认)。
DESIGN.md 升 v2.6.8:头部版本行、组件表 Toggle/Slider 行补"标签 semibold"、§七快捷键行布局条目改写为 v2.6.8 两行式口径。

**第十七轮用户真机验收清单**:
1. 设置页所有设置项标签(自动回复/快捷键/标准回答优先/历史相似度阈值/标准回答阈值/保留期天数/导出包含记忆数据)均为**加粗**;
2. 快捷键行:「Ctrl + Enter」键帽与「修改」按钮**同高**,「修改」位于该行**最右**;
3. 点「修改」进入录入态、Esc 取消等原有行为不变。

---

## 〇-P、2026-09-15 第十六轮:设置页开关/滑杆组件重设计 + 文案精简(单测 243/243,tsc 0 错,build 过,亮暗截图评审)

用户:「设置页的开关组件和滑轨组件更换一下。将开关放置在右侧。简化各项的说明文案」。

| 项 | 内容 |
| --- | --- |
| **Toggle 重设计(开关右置)** | 行式布局:**文字块居左(flex:1)、开关居右(flexShrink:0)垂直居中**,整行可点;轨道 36×20、滑块 16(design.ts size 令牌)。选中 accent(悬浮 accentHover);未选中走**新增主题令牌 `switchTrack`/`switchTrackHover`**(浅 #c6c8cc→#b3b6bc,深 rgba(255,255,255,.16→.24),比 inputBorder 深一档、悬浮再深给出可点暗示)。**滑块位移 + 按压拉伸微交互(顺拖动方向 +2px)全由 popup RESET_CSS 按 `[aria-checked]`/`:active` 驱动**(React 只声明结构,knob 无内联 left —— 内联会压过 :active;新增 `motion.emphasized = .2s cubic-bezier(.2,0,0,1)` 减速曲线落定)。键盘可达原样保留(tabIndex + Enter/Space + focus-visible 焦点环)。TDD:`toggle.test.tsx` 增 2 条(开关为 label 内最后元素=视觉右侧/knob 无内联 left 的 CSS 驱动契约),原 4 条键盘测试不动全过 |
| **Slider 重设计(换掉原生滑轨)** | 原生 `accentColor` 滑轨换**自定义填充轨道**:4px 圆轨按值填充(accent 到当前值硬切,百分比 =(value-min)/(max-min) 由组件内联注入 linear-gradient)+ **14px 白圆 accent 描边拇指钮**(RESET_CSS `.pddcs-slider::-webkit-slider-thumb`/`::-moz-range-thumb`,悬浮 scale 1.12、按住 1.22,focus-visible 焦点环);数值行不变(tabular-nums 右对齐)。TDD:新 `slider.test.tsx` 3 条(标签+格式化值渲染/填充百分比硬切/拖动经原生 value setter 触发 onChange) |
| **文案精简** | 各项 desc 压成一行口径:自动回复「开:点「AI回复」或快捷键直接填充第一条;关:弹面板人工选择」;标准回答优先「标准回答命中时置顶,建议开启」;导出包含记忆数据「额外导出问答记录与回复(向量导入后自动重嵌)」;导入脚注「重复内容自动跳过,不覆盖本地编辑;版本不符将拒绝导入。」;存储脚注「问答按保留期自动清理;清空立即生效,不可撤销。」。快捷键行与「关于」合规文案不动;录入态提示「请按下新的快捷键」前缀保留(verify-ui 断言依赖) |
| **验收脚本:补全 verify-ui 末步** | **勘误**:第十五轮报"verify-ui 15 项 PASS"实为 15 项断言通过但脚本末步(第 207 行找第三轮时代常驻「取消」钮)超时退出、未打出合计行 —— 当时只 grep PASS 行漏看了。本轮把该步改写到当前 UI 语义(标准回答行「填充」钮与悬浮图标钮同取 inline 档等高),verify-ui 现在**完整跑完 16/16** |

验收证据:单测 **243/243**(28 文件,+5);`tsc --noEmit` 0 错;`plasmo build` 过;`verify-ui` **16/16 完整通过**;
截图人工评审:`logs/ui-0915-shot-settings-light.png` / `-dark.png`(开关右置、文案一行化、滑杆填充轨;注意 shots-tabs TAG 默认 `shot`,v265 文件是第十三轮旧图)、
`logs/probe-slider.png`(滑杆放大:33% 蓝色硬切填充 + 白圆蓝边拇指,渐变确认实际渲染)。
DESIGN.md 升 v2.6.7:头部版本行、组件表 Toggle/Slider 行重写。

**第十六轮用户真机验收清单**:
1. 设置页:「自动回复」「标准回答优先」「导出包含记忆数据」三个开关都在**行右侧**;整行点击可切换,悬浮轨道变色;
2. 点住开关拖一半松手/快速连点:滑块动画顺滑、按压时有轻微拉伸感;
3. 键盘 Tab 聚焦开关,Enter/Space 可切换,有蓝色焦点环;
4. 三个滑杆:轨道左段蓝色填充随值变化,拇指钮白圆蓝边,悬浮/按住会放大;拖动松手后约半秒自动保存(右上弹「已保存」);
5. 亮/暗主题各看一遍设置页:开关关轨、滑杆轨道在暗色下清晰可辨;
6. 各项说明文案已是一行短句,无信息缺失感。

---

## 〇-O、2026-09-15 第十五轮:用户三反馈(单候选直填修正 / 记忆卡片金标钮移位 / 文件夹改名入口常驻,单测 238/238,tsc 0 错,build 过)

| 项 | 内容 |
| --- | --- |
| **② 排查(用户:设置没开自动回复,点「AI回复」却直接填充,是因为只有一个答案吗)** | **是,用户猜测正确**。原设计 `pddUiLogic.decideUiAction`:`directFillEnabled \|\| suggestions.length === 1` → 直填 —— "关 + 单候选免面板直填"是 P2 时代的原始设定(单候选弹面板嫌绕),但与开关语义冲突:用户关掉「自动回复」的预期是**绝不自动填**。修复:移除单候选例外,关 → 无论几条候选都弹推荐回复面板(点选或 Enter 填充),开 → 行为不变(直填第一条)。两条路径(点击「AI回复」/快捷键)语义从此一致。设置页开关描述同步写明两条路径与"均不自动发送"。TDD:`pddUiLogic.test.ts` "开关关+单候选"用例改为断言弹面板(RED→GREEN) |
| **① 界面(用户:设置标准回答按钮移到下方删除按钮右侧)** | `MemoryListTab`:金标控件(主钮/已设徽标)抽为 `goldenControl`;**单回复卡片**移到底部操作行、紧挨「删除」右侧(含"已设为标准回答"绿徽,一并跟随);**多回复卡片**仍在各回复行内(放底部无法区分对应哪条回复)。TDD:新 `memoryGoldenBtnPlacement.test.tsx` 3 条(单回复同容器且在删除后/多回复逐回复携带/单回复金标显徽标) |
| **③ 文件夹(用户:父文件夹支持修改文件夹名称)** | **改名功能本就存在且无父夹限制**——v2.6.1 就给过两代入口(悬浮铅笔图标钮 + 双击文件夹名,DESIGN.md 载明当时用户也提过"支持修改名称"),但纯悬浮显现不可发现,用户再次提出。修复:分区头的「重命名/删除」图标钮**常驻可见**(不再挂 `.pddcs-row-ops` 悬浮显隐;「未分类」仍无此二钮),双击改名保留为快捷方式。TDD:新 `folderRename.test.tsx` 2 条(入口不悬浮也可见且普通文件夹都有/点击出预填输入框且未分类无入口);e2e 新 `scripts/verify-folder-rename-2026-09-15.mjs` 3/3(常驻可见→点出预填表单→保存生效) |
| **验收脚本** | 重做随第十四轮回滚消失的 `verify-ui-2026-09-15.mjs` 两处陈旧选择器修复(「新建根文件夹」→「新建文件夹」、占位符「根文件夹名称」→「文件夹名称」,第八轮改名未回写所致) |

验收证据:单测 **238/238**(27 文件,+5);`tsc --noEmit` 0 错;`plasmo build` 过;
`verify-ai-popup-2026-09-15.mjs` 13/13;`verify-ui-2026-09-15.mjs` 15 项 PASS;
`verify-folder-rename-2026-09-15.mjs` 3/3;截图人工评审:`logs/ui-0915-memory.png`(底部操作行 [删除][✓已设为标准回答])、
`logs/ui-0915-folder-rename.png`(「售后服务部」头部铅笔/删除钮常驻)。
DESIGN.md 升 v2.6.6:头部版本行、§文件夹页重命名入口定稿、§记忆卡片金标钮位置、§七-C 开关语义修正。

**第十五轮用户真机验收清单**:
1. 设置页确认「自动回复」**关** → 聊天页点「AI回复」:单条候选也应**弹推荐回复面板**(不再直接填充),点候选或按 Enter 才填入;
2. 同状态按 Ctrl+Enter:同样弹面板(与点击路径一致);
3. 打开「自动回复」→ 点「AI回复」:直接填充第一条(原行为,不回归);
4. 记忆页单回复卡片:底部操作行 [删除] 右侧是「设置标准回答」;点击后原位变「✓已设为标准回答」;
5. 多回复卡片(若有):各回复行内仍有各自按钮;
6. 文件夹页:文件夹标题栏右侧**不悬浮即可见**铅笔(重命名)与垃圾桶(删除);点铅笔改名、双击名也可改名;「未分类」无这两个钮;
7. 亮/暗主题各扫一眼记忆页与文件夹页,无布局跳色异常。

---

## 〇-N、2026-09-15 第十四轮:回滚界面改版(恢复至 837b1c8,单测 233/233,tsc 0 错,build 过)

用户:「回滚修改前的版本」。当日早些时候的第十四轮界面改版(飞书蓝 #3370FF 主题、开关/滚动条重设计、推荐回复面板重设计,提交 74a82b0 / c062800 / 50067a7 / 13820f4)整体撤销。

- 方式:`git revert --no-commit 74a82b0^..HEAD` 一次性反向应用 4 个提交 → 单提交 `e907e93`;**不改写已推送历史**,远端正常快进。
- 保留价值:被撤销的 4 个提交仍在历史中,日后若想恢复该界面可 cherry-pick 或 revert-this-revert。
- 重建:`plasmo build` 已重跑,`build/chrome-mv3-prod` 为回滚后产物(用户重载扩展即回到旧界面)。
- 验收:tsc 0 错;单测 **233/233**(25 文件,随撤销的 8 个新测试回到第十三轮数量);build 过。
- 注:被撤销工作留下的唯一残留是 `verify-ui-2026-09-15.mjs` 的选择器修复(第八轮改名所致),该修复随回滚一并消失,已于第十五轮重做。

---

## 〇-M、2026-09-15 第十三轮:新建按钮公共组件 + 招呼语命中排查(单测 233/233,tsc 0 错,build 过,截图验收)

| 项 | 内容 |
| --- | --- |
| **界面(用户要求:新建按钮抽公共组件)** | `ui/components.tsx` 新增 `CreateBtn`(加号图标 + 文字,内部走 `Btn` primary / `.pddcs-btn`,高 controlH.form):知识库页「新建条目」与文件夹页「新建文件夹」统一同款,文件夹页原手搓 button(自写 hover/配色)移除。TDD:`tests/unit/ui/createBtn.test.tsx` 3 条(渲染+点击回调/禁用透传/title 透传),RED(组件未导出)→ GREEN → 两页替换,单测 230→233 |
| **排查(用户报告:检索"产品介绍"却命中"你好"招呼语问答)** | 结论:检索本身按设计工作,现象由三个机制叠加(按确定性排序):① **问答锚确实是"你好"** —— 分段规则"买家连续消息并段,客服回复/3 分钟 idle 即关闭",买家先发"你好"、欢迎语先回了一句 → 落库 question="你好"、回复=招呼语,这是真实历史;② **查询可能带着"你好"** —— 「AI回复」钮把点击行及其上方连续买家气泡合并为 query,快捷键取最新可见行再向上合并;若"你好"气泡紧邻产品问题上方,query 实为"你好\n产品介绍…",招呼语记录在向量路与 BM25 路(二字面命中)都天然高分;③ **短文本枢纽效应 + 时间配额** —— bge-small-zh 对两字短锚的余弦普遍偏高,0.5 历史阈值可能拦不住;且历史配额为"取最近 2 条(按回复时间,与相关度无关)"(2026-09-15 用户口径),新招呼语必占一席。**真机定位法**:记忆页看该问答回复是否即候选内容(验①);复现时留意"你好"气泡是否在点击行上方(验②);设置里把历史阈值临时调 0.7 再试,候选消失则③占主导。**处置选项待用户拍板**:A 短锚(<4 字)降权/不入源(需防误伤"多少钱"等正当短问题);B 历史配额改相关度优先(与既有口径冲突);C 纯招呼语段不落盘(会丢"招呼语→欢迎话术"沉淀);D 维持现状 |

验收证据:单测 **233/233**(25 文件,+3);`tsc --noEmit` 0 错;`plasmo build` 过;
`shots-tabs.mjs v265` 截图人工评审:文件夹页「+ 新建文件夹」与知识库页「+ 新建条目」同款黑底胶囊,亮暗两主题无回归。

---

## 〇-L、2026-09-15 第十二轮:评审清单第三批(工程5b + PM2/6a/7 + 审计小项,全部完成,单测 230/230,tsc 0 错,build 过,截图验收)

用户:「继续第三批」。§〇-I 清单剩余的代码项全部收官(仅 PM5 快捷键冲突为真机验证项,见文末)。

| 项 | 改动 |
| --- | --- |
| **工程5b(检索全表 toArray 劣化)** | 新文件 `background/retrievalCache.ts`:三源"已嵌条目"(id/kind/锚文本/questionTs/向量)驻留 SW 内存,`searchSuggestions` 改走缓存 —— 命中时不触库(原每次检索全量 toArray 三表含向量,几万条时 IndexedDB 结构化反序列化是主要劣化点)。**失效模型**:db 层 10 个改变"已嵌三源"集合的写路径(三个 update*Embedding / deleteQaWithReplies / clearSelfTestRecords / clearQaMemory / purgeExpired / deleteGolden / updateKnowledge(enabled 开关)/ deleteKnowledge / deleteKnowledgeByDoc)调 `invalidateRetrievalCache()`,下次检索整体重建。缓存只存锚+向量不存正文 —— 候选正文按过阈 id `bulkGet` 即时取,IO 与候选数成正比。TDD:缓存模块 11 条(三源合并/未嵌不入/命中不再读库/失效重读/7 条写路径失效);`search.test.ts` 新增编排层测试 5 条(mock offscreen 确定性向量:阈值门控/历史/金标准/写入即失效端到端)—— 编排层此前是审计点名的测试盲区 |
| **PM2(记忆列表静默截断)** | `listQaRecordsPage(offset, limit)`(questionTs 倒序 + 排除自检,返回 rows+total);`GET_MEMORY_LIST` 增 `payload?: {offset?, limit?}`,响应增 `total`(排除自检,与头部统计同口径)与 `hasMore`。记忆页底部「加载更早(已显示 X/Y 条)」ghost 钮追加下一页(offset=已加载条数),载完显示「已显示全部 Y 条」caption。TDD:panel 4 条(首页/末页/total 排除自检/缺省兼容)+ 组件 2 条(mock sendMessage:点击携带 offset/hasMore=false 无入口) |
| **PM6a(存储用量与清空)** | db `clearQaMemory()`(事务清 qa+replies,返回条数,失效检索缓存)+ 消息 `CLEAR_MEMORY_DATA` + 设置页「存储」卡:五表计数行 + `navigator.storage.estimate()` 用量行(无 API 隐藏)+「清空问答数据」danger 钮 → confirmRow 式二次确认;**只清问答与回复,标准回答/知识库/文件夹保留**。TDD:panel 3 条(清空且长期资产保留/缓存失效/空库返回 0) |
| **PM7(遗留子夹拍平)** | db `flattenSubfolders()`(事务:子夹金标准上移父根夹,父夹失联归「未分类」,删除子夹)+ 消息 `FLATTEN_FOLDERS` + 文件夹页横幅(存在 parentId≠null 存量子夹时渲染:虚线边框条 + 「一键拍平」→ confirmRow 二次确认,拍平后自然消失)。TDD:panel 3 条(上移父夹/失联归未分类/无子夹不动) |
| **审计:导入无事务** | `importData` 写库阶段(folders/goldens/knowledge/qaRecords/replies 五表 bulkAdd)整体包进一个 Dexie 事务;计划逻辑(读)前置不变,重嵌排队移到事务提交后(回滚不留幽灵队列)。中途失败(如手工编辑的文件含重复 id)从"半成品库"变为整体回滚。TDD:`transferImport.test.ts` 2 条(同 id 冲突 → error 且先写的文件夹被回滚/成功导入计数不变) |
| **审计:未用依赖** | `minisearch`、`@plasmohq/storage` 全仓零引用(src/tests/scripts/config),pnpm remove |

截图脚本 `shots-tabs.mjs` 本轮增强:预置 105 条填充问答(验证分页:100/106 截图)、
末尾追加记忆页滚动到底(`memory-paged`)与设置页存储卡(`settings-storage`)两张附加截图。

验收证据:
- 单测 **230/230**(vitest,24 文件;本轮 +30:retrievalCache 11、search 编排 5、panel 分页 4、清空 3、拍平 3、导入事务 2、分页组件 2);
  `tsc --noEmit` 0 错;`plasmo build` 过;
- `scripts/shots-tabs.mjs v264` 截图人工评审:记忆页头部「问答 106」与底部「加载更早(已显示 100/106 条)」同屏口径一致;
  文件夹页拍平横幅(亮暗)正常;设置页存储卡计数/用量/清空钮齐全;暗色主题无回归。

### 用户真机验收清单(第十二轮)

1. 记忆页 >100 条时底部出现「加载更早(已显示 100/N 条)」,点击追加 100 条;全部载完显示「已显示全部 N 条」;头部统计与 Y 一致。
2. 设置页「存储」卡:五个计数与各页签数据一致;「已用 x MB(配额约 y GB)」显示合理;「清空问答数据」→ 确认后问答与回复清空、标准回答/知识库/文件夹保留,聊天页检索仍正常(标准回答可召回)。
3. 文件夹页有遗留子夹(如「退换货」)时显示拍平横幅;「一键拍平」→ 确认后子夹消失、其下标准回答出现在父文件夹;无子夹后横幅不再出现。
4. (性能,大数据量场景)记录数上万后首次检索后,后续检索响应应明显快于此前版本(检索走内存缓存);新增/删除标准回答、知识库启停、删除问答后紧接着的检索结果如实变化(缓存已失效)。
5. **PM5(遗留真机项)**:在 PDD 工作台聊天页按 Ctrl+Enter(当前快捷键),确认与平台自身快捷键(如平台 also 用 Ctrl+Enter 发送消息时)是否冲突;如冲突,在设置页「修改」改用含 Alt 的组合键即可。

---

## 〇-K、2026-09-15 第十一轮:评审清单第二批(工程5 + PM1/3/4/6 + 设计1/5/6,全部完成,单测 200/200,tsc 0 错,build 过,截图验收)

用户:「继续第二批」。沿用第一批工作流:TDD → 全量验证 → 文档回写 → 分粒度提交推送。

| 项 | 改动 |
| --- | --- |
| **工程5(RRF 栈溢出)** | `retrieval.ts#rrfFuse` 的 `Math.max(-1, ...rankLists.flat())` 改循环求最大下标 —— spread 实参有 V8 上限,几万条候选过阈时直接 RangeError(RED 测试:20 万下标先复现栈溢出)。检索全表 toArray 的性能劣化部分留待后续(需要向量索引缓存,改动面大,单独一批) |
| **PM1(金标徽标持久化)** | `GET_MEMORY_LIST` 按 `goldens.sourceReplyId` 反查回带 `goldenId`(`MemoryReplyItem` 新增可选字段);记忆页 `goldenReplyIds` 从数据派生(useMemo),删除会话级 state —— 重开 popup、文件夹页取消后徽标都如实反映库内状态;设成功后 `await load()` 拉最新。新文件 `tests/unit/background/panel.test.ts`(fake-indexeddb + resetModules) |
| **设计1(覆盖层跟随主题)** | 覆盖层 CSS 抽为纯函数 `utils/overlayTheme.ts#buildOverlayCss(tk)`(+`parseThemeMode` 容错解析、`THEME_STORAGE_KEY`/`POPUP_W` 单处维护);`pdd-ai-button.ts` 删模块顶 `lightTheme as tk` 定格,启动读 `chrome.storage` 的 `pddcs:theme`、`storage.onChanged` 实时重建 `<style>`;弹窗阴影改用 `tk.shadow`(暗色有专属深阴影);toast 保持深底白字不随主题。vitest 配置补 `esbuild: { jsx: 'automatic' }`(tsconfig `jsx: preserve` 会让 .tsx 测试退回 classic 运行时而源码只有 type-only React 导入) |
| **PM4(嵌入失败文案)** | 「嵌入失败,重启扩展后重试」→「嵌入失败,后台将自动重试」(工程1 之后 -1 记录已被补嵌扫描自动重试,原文案与机制矛盾) |
| **PM6(Notice 自动消失)** | `Notice` 增 `onDismiss` 可选回调:成功提示 4s(`NOTICE_OK_AUTO_DISMISS_MS`)自动回调父级清 state,失败提示常驻;计时以 msg 变化重置(拖滑杆连弹以最后一次为准)。四页签全部传入 `onDismiss={() => setMsg(null)}`。RED 测试 3 条(到时回调恰一次/失败常驻/文本变化重置计时) |
| **设计5(导航激活指示)** | 激活页签加 2px×14 左侧短指示条(贴导航栏左缘,`tk.text` 色)—— 不画背景块,原"颜色+描边粗细"激活态几乎不可辨 |
| **设计6(可达性与悬浮态)** | ① Toggle 键盘可达:`tabIndex=0` + Enter/Space 切换(preventDefault 防滚动、stopPropagation 防外层 label 双翻)+ `.pddcs-switch:focus-visible` accent 焦点环(4 条 RED 测试);② Btn 四 variant 全有悬浮底色:新增 `errorHoverBg` 令牌(浅 `#fde3dc`/暗 `rgba(255,114,98,.22)`),ghost/default 悬浮面色提亮;③ 知识库手工条目去徽标(本页即知识库,徽标只标文档块),删 BookOpenIcon 引用 |
| **PM3(元数据)** | README.md 重写为本工具说明(原为上游 Personal AI Memory 的带商店徽章营销页);删除上游多语言 README ×8(`README-multi-lan/`);package.json version 0.1.0 → **0.10.0**,描述去旧称「金标准」、补「发送永远由人工完成」 |

验收证据:
- 单测 **200/200**(vitest,20 文件;本轮 +16:RRF 1、panel 2、Notice 3、Toggle 4、overlayTheme 6);
  `tsc --noEmit` 0 错;`plasmo build` 过;
- `scripts/shots-tabs.mjs v263` 四页签 × 亮/暗 8 张人工评审 —— 导航指示条四页签清晰可辨、
  知识库手工条目无徽标、暗色按钮/开关/滑杆渲染正常。

### 用户真机验收清单(第十一轮)

1. popup 切深色 → 聊天页「AI回复」按钮与推荐回复面板**即时**变深色(无需刷新);切回浅色同理;首次安装未设过主题时默认浅色。
2. 左侧导航当前页签在图标左侧有一条 2px 短指示条,亮暗两主题下都可见;切换页签跟随。
3. 设置页连续拖动滑杆:「已保存」提示约 4 秒自动消失,不再堆积推挤布局;失败类提示不自动消失。
4. 记忆页把某条回复设为标准回答 → **关掉重开 popup**,该回复仍显示「已设为标准回答」;在文件夹页取消后重开,徽标消失。
5. Tab 键能把焦点移到设置页的开关上,Enter/Space 可切换,键盘聚焦时有蓝色焦点环。
6. 知识库手工条目不再显示「知识库」徽标;.md 文档块仍显示「文档」徽标。
7. (数据正确性,长会话场景)标准回答/知识条目嵌入失败时,提示文案为「嵌入失败,后台将自动重试」,稍后自动恢复为向量生成中→消失。

---

## 〇-J、2026-09-15 第十轮:评审清单第一批(工程1-4 + 设计2/3/4,全部完成,单测 184/184,tsc 0 错,build 过,截图验收)

用户挑选 §〇-I 清单第一批:四项数据正确性 + 三项界面一致性。全程 TDD(先写失败测试再实现)。

**工程1 — 嵌入失败(-1)记录重试**(§〇-I 工程#1)
- `db.ts` getPending* 三方法改查 `.anyOf([0, -1])`,新增 `excludeIds` 参数(同一次补嵌运行内已失败者不再入批,防"查到→失败→再查到"死循环)
- `syncEmbeddings.ts` 补嵌循环维护 failedGoldens/failedKb/failedQas 三个 Set,逐批累加并传入查询
- 测试 +5:db 三源 -1 返回/排除、补嵌循环 ★失败下次运行重试/同轮终止/整批 reject

**工程2 — msgId 幂等落库(库级锚,SW 重启防御)**(§〇-I 工程#2)
- Dexie **version(4)**:qaRecords +`msgId` 索引、replies +`msgId` 索引;`findQaByMsgId` 新增,`findReplyByMsgId` 由全表 filter 升级为索引查询
- QaRecord +`msgId?`(问题段首条买家消息);segmenter 透传 `CloseQuestionCtx.firstMsgId`(开段时定,后续买家消息不覆盖)
- `pddCapture` 三道闸:消息入口(内存 seen 未命中再查库)、closeQuestion(同 msgId 复用既有问答)、attachReply(同 msgId 回复不重挂)
- 测试 +5:segmenter 首条 msgId 透传×2、db 索引查询、capture 重放拦截(★)/落库携带 msgId

**工程3 — 未结段持久化 + idle 关段**(§〇-I 工程#3)
- `PddSegmenter` +`exportState()/restoreState()`(链上串行、深拷贝、纯 JSON 可入 storage.session)
- `pddCapture` +`persistSegmenterState()`(每次 ingest 落盘后写 `chrome.storage.session`)/ `restoreSegmenterState()`(SW 启动恢复,`index.ts` 每次唤醒调用)
- content 侧 **idle 定时器**:3 分钟无新消息发 `{kind:'idle', sessionKey}`(此前 idle 路由在 SW 端但无人发送);切会话不发过期 idle,pagehide 清定时器
- 测试 +6:segmenter 快照导出/restore 往返/空快照 no-op、capture 快照写入/恢复后配对落盘/idle 事件关段(chrome mock 补 `storage.session` 状态后端)

**工程4 — 捕获容器重挂自愈**(§〇-I 工程#4)
- 兜底轮询检测 `listEl.isConnected`,Vue 重挂脱离文档 → 清空本地 seen(重放由 SW 内存+库级 msgId 双层去重兜底)→ 重新等容器挂 MutationObserver

**设计2/3/4 — 一致性对齐**(§〇-I 设计#2/3/4;DESIGN.md → v2.6.2)
- 设计2:「新建文件夹」描边钮 → 黑底白字主按钮(与知识库「新建条目」同权重)
- 设计3:知识库卡片次级操作(复制/编辑/停用/删除)收进 `.pddcs-row-ops` 悬浮层(与文件夹页同款),「填充」保持常显,删除确认时常驻;`Card` 组件 +`className` prop
- 设计4:危险按钮统一软底色(errorBg 底 + errorText 字)——确认删除行内钮与 `BtnMini` danger 两处旧"纯 errorText 底+白字"形态替换,与 components `Btn` danger 同形

**验证**:单测 184/184(本轮 +16)、tsc 0 错、plasmo build 过、shots-tabs 截图亮暗 8 张目检(主按钮/悬浮层生效)。

### 第十轮用户真机验收清单
1. 加载 build/chrome-mv3-prod,自检通过后正常聊几句 → 断言:问答正常入库
2. 让 SW 休眠(等 40s 不操作)→ 客服回复到达 → 断言:回复正确挂到休眠前的问题(未结段恢复生效)
3. 聊完后 3 分钟不操作 → 断言:未回复的问题以"无回复问题"落盘(idle 关段生效)
4. 刷新 PDD 页面(Vue 重挂)→ 断言:新消息仍能捕获;历史消息不重复入库(msgId 库级幂等)
5. 文件夹页:「新建文件夹」为黑底主按钮;知识库卡片:静止只见「填充」,悬浮行尾显现复制/编辑/停用/删除
6. 知识库/文件夹删除确认:按钮为浅红底红字(软底色,非纯红块)

---

## 〇-I、2026-09-15 三方评审(工程师/产品/设计师,只评审未改码,清单待用户挑选执行)

用户要求以 工程师+产品经理、设计师 两个视角评审现状。方法:通读九轮提交 + 四页签亮暗 8 截图(v262)+
UI 源码全读(theme/components/四页签/pdd-ai-button)+ 子代理后台代码审计(25 条,file:line 齐备)。

**工程师视角(高优先,数据正确性)**:
1. 嵌入失败(-1)记录永不重试:补嵌扫描只查 `hasEmbedding=0`(db.ts:300-310),注释承诺的"-1 下次启动重试"实际断的
2. msgId 去重纯内存(SW 休眠失忆)+ 5 分钟合并窗,超窗历史重放会重复入库;msgId 落库无索引(pddCapture.ts:114)
3. 未结问题段纯内存,SW 休眠即丢;且 content 从未发 `idle` 事件,文档承诺的 idle 关段不触发(pdd-chat-capture.ts:78,153)
4. 消息列表容器被 Vue 重挂后捕获静默死亡(interval 不检测 isConnected 重挂)(pdd-chat-capture.ts:123-148)
5. 检索每次全量 toArray 三表含向量,几万条后劣化;RRF spread 有栈溢出风险(retrieval.ts:129)
其余:同文本消息漏采、导入无事务、setTimeout 重试链不可靠、scroll 未节流、逐条嵌入 IPC、消息零校验、
测试盲区(补嵌循环/编排层)、未用依赖 minisearch/@plasmohq/storage、无 ESLint、manifest 双处维护等 20 条(详见面包回复)。

**产品经理视角**:
1. 记忆页"已设为标准回答"仅本次会话可见,重开 popup 复原为可再点 → GET_MEMORY_LIST 应返回 golden 标记,持久徽标
2. 记忆列表静默截断 100 条(panel.ts:20),头部统计却是全量数,无"更早记录"入口
3. README/package.json 描述还是上游"Personal AI Memory"+旧称"金标准",version 0.1.0 与九轮迭代脱节
4. "嵌入失败,重启扩展后重试"文案与实际机制(不重试)矛盾,且无手动重试入口
5. 快捷键 Ctrl+Enter 捕获阶段劫持:需真机确认与 PDD 工作台自身快捷键(如 Ctrl+Enter 发送)是否冲突
6. 设置页无存储用量/清空数据;Notice 永不自动消失,设置页每次拖滑杆都弹"已保存"推挤布局
7. 遗留子文件夹(如"退货费")无"拍平"迁移入口,长期占位

**设计师视角**:
1. 聊天页覆盖层写死浅色主题(pdd-ai-button.ts:35 `lightTheme as tk`),不跟随 popup 主题设置 → 应读 storage + onChanged
2. 新建按钮权重不一致:知识库"新建条目"黑色主钮 vs 文件夹"新建文件夹"描边钮(FoldersTab.tsx:849 vs KnowledgeTab.tsx:249)
3. 操作揭示不一致:文件夹悬浮显现图标钮 vs 知识库卡片 5 个文字钮常驻;删除确认两种形态(红实心 confirmRow vs danger Btn)
4. 记忆页每条回复一个黑色主钮,一卡 N 个黑块视觉过重 → 可降 default 档或悬浮显现
5. 导航激活态仅颜色+0.6px 描边差,几乎不可辨;用户禁背景块 → 可用 2px 左侧短指示条(非背景)
6. Toggle 无键盘可达(role=switch 但不可聚焦);Btn danger/ghost 无悬浮态;知识库手工条目"知识库"徽标冗余

---

## 〇-H、2026-09-15 第九轮:用户 2 项反馈(全部完成,单测 168/168,tsc 0 错,build 过,截图评审)

用户反馈:① 记忆页问答卡片的折叠图标移至右侧,文字内容向左移动;② 文件夹页支持修改文件夹名称。

改动:

| 文件 | 改动 |
| --- | --- |
| `src/popup/MemoryListTab.tsx` | 问题行重排:问题与元信息居左,折叠钮移到行尾(整行仍可点);展开区不再为折叠钮留缩进(`marginLeft` 删除),与问题文字同左基线 |
| `src/popup/FoldersTab.tsx` | 文件夹重命名新增**双击文件夹名**入口(资源管理器直觉);悬浮「重命名」图标钮保留,tooltip 注明双击;「未分类」不可改名 |
| `docs/DESIGN.md` | v2.6.1 头部补记(第八轮两处编辑未落盘,本轮补齐)+ 记忆卡片/重命名入口新口径 |

排查说明:重命名功能(后台 `RENAME_FOLDER` + 悬浮铅笔钮)此前已存在,用户未发现 —— 根因是悬浮显现太隐蔽,
故补双击入口而非新造功能。

### 用户真机验收清单(第九轮)

- [ ] 记忆卡片:问题文字顶格居左,折叠箭头在卡片头右侧;点击箭头或整行可折叠/展开
- [ ] 展开后的回复区与问题文字左对齐
- [ ] 文件夹行**双击**直接进入改名(输入框原位出现);悬浮铅笔钮也可改名;「未分类」双击无反应
- [ ] 亮暗两主题渲染正常

---

## 〇-G、2026-09-15 第八轮:用户 3 项反馈(全部完成,单测 168/168,tsc 0 错,build 过,截图评审)

用户反馈:① 删除子文件夹设置,仅保留一级文件夹;② 「新建根文件夹」更名「新建文件夹」;
③ 设置页快捷键行控件与说明挤压在一起,需要换行。

改动(纯 UI,无数据层变更):

| 文件 | 改动 |
| --- | --- |
| `src/popup/FoldersTab.tsx` | 移除分区头「新建子文件夹」图标钮与分区内联新建表单;`submitCreate` 固定 `parentId: null`;遗留子夹数据照常展示(同构行)与删除;「新建根文件夹」→「新建文件夹」(含占位符/空态文案) |
| `src/popup/SettingsTab.tsx` | HotkeyRow 改 `flexWrap` 布局:第一行 = 标签 + kbd + 修改钮;说明「在聊天页按此键唤起推荐回复」与录入提示各占独立一行(`flexBasis:100%`) |
| `docs/DESIGN.md` | v2.6.1:文件夹页「仅一级文件夹」定稿口径、快捷键行布局规范 |

设计口径:文件夹扁平化(v2.6.1)—— 本工具使用密度下两层树过重,遗留子夹仅保兼容展示。
背景层 `panel.ts` 的「最多两层」防御约束保留不动(UI 已无入口)。

### 用户真机验收清单(第八轮)

- [ ] 文件夹页无任何「新建子文件夹」入口;按钮文案为「+ 新建文件夹」
- [ ] 已有的子文件夹(如「退货费」)仍正常展示、可删除
- [ ] 设置页快捷键行:标签/kbd/修改一行,说明文字独立一行不挤压
- [ ] 亮暗两主题下以上三处渲染正常

---

## 〇-F、2026-09-15 第七轮:用户 4 项反馈(全部完成,单测 168/168,tsc 0 错,build 过,四页签亮暗截图评审)

用户反馈:① 侧边栏图标状态仅展示图标,图标后不要有背景;② 文件夹页树状太乱,重新设计;
③ 亮暗切换时按钮颜色转换要有规范,暗色按钮参照示例图(中性深灰底+边框、悬浮提亮);
④ 快捷键呼出的窗口与推荐回复窗口一致,组成固定为 标准回答全部 + 历史最近 2 + 知识库 1。

1. **导航图标彻底去背景**(`popup/index.tsx`):v2.5 只去掉了默认态背景块,悬浮仍走
   `--pddcs-rail-hover` 灰底;本轮连悬浮底色一并移除 —— `.pddcs-rail-btn` 任何状态只有图标本身,
   激活态依旧用图标颜色(`text` vs `textMuted`)与描边粗细(2.4 vs 1.8)表达;
   CSS 变量桥随之删掉 `--pddcs-rail-hover`。
2. **文件夹树二次重设计**(`FoldersTab.tsx`):v2.4 的 `borderLeft` 引导线包裹层与容器边框、
   行分隔线叠在一起显乱,本轮移除;层级只靠三件事表达 ——
   统一缩进节奏(`padding-left = 12 + 16×深度`,分区头/子夹行/标准回答行/内联表单/空态全部同基线)、
   字重字号(根夹 semibold·34 高标题栏,子夹 medium·30 高透明行)、根夹标题栏底色。
   子夹与根夹行结构完全同构(chevron+图标+名称+计数+悬浮操作),子夹仍排在父夹内容之前。
3. **按钮亮暗切换规范**(`theme.ts`,DESIGN §二 新增规范表):两种主题同构映射,
   default = 面色底+边框、悬浮提亮一档;primary = 主色底白字、悬浮再提亮一档。
   暗色主按钮 `#0D99FF`(蓝)→ **`#45484D` 中性深灰**(参照用户示例图,蓝与中性色系脱节),
   悬浮 `#53565B`;Figma 蓝只保留给焦点/开关/滑杆等状态。历史沿革:近白(过亮)→ 蓝(跳色)→ 中性灰(定稿)。
4. **推荐回复固定配额组成**(`retrieval.ts` + `pddUiLogic.ts`):
   - `assembleSuggestions` 弃用「maxSuggestions 截断 + 类别保障补位」,改为**类别配额装配**
     (`PANEL_QUOTA = { history: 2, knowledge: 1 }`,标准回答不限 —— 同问题本身上限 3):
     在折叠后胜者池上选取,标准回答全部展示、历史按**回复时间**取最近 2 条(与相关度无关)、
     知识库取相关度最高 1 条;「标准回答优先」开关只影响排序不影响配额;
   - `decideUiAction` 不再 slice top-3(POPUP_MAX 删除):面板展示检索侧返回的完整配额组成;
   - 快捷键唤起与点击「AI回复」共用 `openPopup`,**天然同构**;
   - 单测重写:原「maxSuggestions 截断」「类别保障补位」两组用例替换为配额用例
     (历史 5 条只留最近 2 且时间倒序 / 知识库多条取相关度最高 / 标准回答 5 条全部展示不截断)。

验收证据:
- 单测 **168/168**(vitest),`tsc --noEmit` 0 错,`plasmo build` 过;
- `scripts/shots-tabs.mjs v26`:四页签 × 亮/暗 8 张截图人工评审 —— 侧边栏纯图标、
  文件夹树缩进干净无引导线、暗色「填充」主钮与「新建根文件夹」同色系(中性灰),蓝只出现在开关/滑杆。

### 用户真机验收清单(第七轮)

1. 侧边栏(亮/暗):四个页签图标与主题切换钮在任何状态(默认/激活/悬浮)下都无背景块。
2. 文件夹页:根夹标题栏 → 子夹缩进行 → 标准回答行,三者左基线连贯;无竖向引导线;
   子夹排在父夹标准回答之前;未分类虚线框置底。
3. 亮暗切换:设置页/文件夹页来回切换,按钮只在中性色明度间变化(浅色黑主钮 ↔ 深色灰主钮),无蓝色按钮。
4. 聊天页:点击「AI回复」与按 Ctrl+Enter 唤起的面板组成一致 ——
   命中的标准回答全部展示;历史回复最多 2 条且是最近的;知识库最多 1 条。

## 〇-E、2026-09-15 第六轮:用户 6 项反馈(全部完成,单测 167/167,tsc 0 错,build 过,验收 13/13 + 15/15)

用户反馈:① 文件夹中的问答卡片居左;② 左侧导航按钮默认态删掉灰色背景只留图标;
③ 暗色模式按钮颜色重新设计;④ 候选面板:底部气泡时面板溢出屏幕 / 改名"推荐回复" / 不展示得分 /
必须展示三类且按 标准答案>历史>知识库 排序 / 增加快捷键(默认 Ctrl+Enter,可在设置录入);
⑤ 快捷键在设置中展示并支持自定义录入。

1. **问答卡片居左**:`FoldersTab` 内容行缩进 28→12px(与容器内其他内容同一基线,子级每层 +12)。
2. **导航极简化**:激活态不再画灰底块与边框,只保留图标 —— 激活态用图标颜色(`text`)与描边粗细
   (2.4 vs 1.8)表达;悬浮底色走 `--pddcs-rail-hover`(未激活不写内联背景,否则 :hover 被压住)。
3. **暗色主按钮重设计**:`btnPrimaryBg` 深色由 `#E6E6E6` → **`#0D99FF`(蓝底白字)**。
   原黑白反转(近白主钮)在深色界面上过亮 —— 这正是上一轮用户抱怨"暗色下为白色"的另一半来源。
   浅色保持黑底白字;DESIGN 配色表已改口径。
4. **推荐回复面板**(`pdd-ai-button.ts`):
   - 标题「AI 候选(n)」→「**推荐回复(n)**」;
   - 得分数字不再展示(对客服无决策价值),同内容×n 保留;
   - **溢出修复**:面板挂载后量 `getBoundingClientRect().height` 向上取整(用 `offsetHeight` 会丢亚像素,
     差 0.1px 恰好压线溢出 —— 两次实测才定位),垂直方向整体夹在视口内;
   - **类别保障**(`retrieval.ts`):面板必须展示 标准答案/历史/知识库(存在且文本未展示过时),
     被折叠/截断吞掉的类别从折叠前候选池补最优代表,按优先级插到应有层级
     (同文本不补,维持"同内容折叠"既定语义,避免重复占位)。
5. **快捷键(默认 Ctrl+Enter,可自定义录入)**:
   - 「自动回复」**关**(原"直接填充",按用户称呼改名):检索**用户最新消息** → 弹推荐回复面板,
     再按 Enter 填充第一条;**开**:检索 → 直接填充第一条;
   - 面板脚注提示「按 Enter 填充第一条」;Esc/点外部关闭即解除;再按快捷键 = 重新检索;
   - 设置页新增快捷键行:`kbd` 展示当前组合 +「修改」进入录入态(修饰键单独按下先等待主键,
     Esc 取消,纯主键不允许录入),`HotkeyRow` 组件;
   - 数据模型:`PddSettings.autoReplyHotkey`(types/memory 默认 Ctrl+Enter),`clampSettings` 校验;
     内容脚本经 `GET_STATS` 初始化 + `chrome.storage.onChanged` 实时生效;
   - 快捷键纯逻辑抽到 `utils/hotkey.ts`(formatHotkey/matchesHotkey/isModifierOnly,+9 单测)。

验收证据:
- `scripts/verify-ai-popup-2026-09-15.mjs` **13/13**:新增 Ctrl+Enter 唤起「推荐回复」面板、
  脚注提示、**面板完整落在视口内**(夹具塞了 12 条消息把最后一条买家消息压到视口底部)、
  再按 Enter 后第一条回复真实填入 `#replyTextarea` 且面板关闭;
- `scripts/verify-ui-2026-09-15.mjs` **15/15**:新增设置页快捷键展示(Ctrl + Enter)、
  「修改」进入录入态、Esc 取消;
- `scripts/shots-tabs.mjs` 重截 8 张(`logs/ui-0915-final-*.png`):导航只留图标、暗色主按钮变蓝、
  设置页快捷键行、问答卡片居左。

### 用户真机验收清单(第六轮)
1. 文件夹页问答卡片贴左
2. 左侧导航只有图标(激活页签靠图标加深加粗区分),悬浮有浅色反馈
3. 切暗色:主按钮(填充/设置标准回答/导出 JSON 等)为蓝底白字,不再是白色
4. 聊天页:靠近屏幕底部的气泡点「AI回复」,面板完整可见
5. 面板标题为「推荐回复(n)」,候选行不再显示得分数字,只保留「同内容×n」
6. 面板同时可见 标准答案/历史/知识库 三类,按 标准答案>历史>知识库 排序
7. 聊天页按 Ctrl+Enter:自动回复关 → 弹面板,再按 Enter 填第一条;开 → 直接填第一条
8. 设置页显示「自动回复」开关 + 快捷键(Ctrl + Enter),点「修改」按下新组合键即生效

---

## 〇-D、2026-09-15 第五轮:用户 5 项反馈(全部完成,单测 158/158,tsc 0 错,build 过,验收 17/17 + 11/11)

用户反馈:① 顶部"问答/回复/标准回答/知识"四个计数不该全堆在上方,要分散到各页面(设置页不展示);
② 文件夹页看着混乱,尤其"文件夹的表示",按设计师身份重新设计;③「新建根文件夹」按钮移到左侧(参考知识库);
④ 面板宽度 +20px;⑤ 暗色模式下有些按钮未转换风格/颜色有问题(如开关在暗色下是白色)。

**先取证再改**:写了 `scripts/shots-tabs.mjs`(造样本库 → 四页签 × 亮/暗色 8 张截图)作为评审底稿,
暗色问题即从截图确认:开关选中态 `tk.text` 在深色下 = `#E6E6E6`,于是"白轨道 + 白圆点",圆点直接消失。

1. **顶部概览按页签分散**(`index.tsx#tabSummary`):记忆 = `问答 n · 回复 m`;文件夹 = `文件夹 k · 标准回答 g`;
   知识库 = `知识 n`;**设置页不展示**(保留行高,避免切页签时内容跳动)。
2. **文件夹页分区容器化重设计**(`FoldersTab`):
   - 根文件夹 = 一张分区容器(1px 边框 + 8px 圆角 + `overflow:hidden`),头部为标题栏
     (常驻 `bgSecondary` 底、展开时底边分隔线、悬浮加深),把"文件夹"和"里面的答复"框在一起;
   - **文件夹优先于内容**:子文件夹移到父夹标准回答**之前**(原先夹在中间,是"混乱"的主因),
     缩进 12px + 1px 引导线,子夹头透明底、字号降档、名称转 muted;
   - 标准回答行缩进对齐文件夹名(`28 + 12×深度`)并加行悬浮底色;
   - 空文件夹给"暂无标准回答"占位;**未分类**容器改虚线边框 + muted,弱化存在感。
3. **工具栏左对齐**:「+ 新建根文件夹」与知识库的按钮行同款(左对齐),内联新建时原位变表单。
4. **宽度 400 → 420**(`size.popupWidth`):文件夹树的层级缩进需要更宽;文档同步。
5. **暗色模式修复**:
   - 开关(Toggle)选中态由 `tk.text` 改 **`tk.accent`** —— 黑白反转只适用于按钮,开关用状态色,
     两个主题下圆点都有对比;DESIGN 的配色表已标注适用边界;
   - 滚动条滑块原硬编码 `rgba(0,0,0,.16)`,暗色下在深底上不可见 → 新增主题令牌 `scrollThumb`
     (亮 `rgba(0,0,0,.16)` / 暗 `rgba(255,255,255,.24)`);
   - 导航图标钮此前**没有悬浮反馈**:原因是未激活项内联写了 `background: transparent`,
     内联优先级高于类选择器 → 改为未激活不写内联背景,`.pddcs-rail-btn:hover` 走 CSS 变量;
   - 静态 CSS 读不到 React 令牌 → 引入 **令牌 → CSS 变量桥**(`--pddcs-scroll-thumb` / `--pddcs-rail-hover`),
     写入规则进 DESIGN §九。

验收证据:
- `scripts/shots-tabs.mjs` 前后各一套 8 张截图(亮/暗 × 四页签),改造前/后可直接对比:
  `logs/ui-0915-{before,after}-<tab>-<theme>.png`。
- `scripts/verify-golden-multi-2026-09-15.mjs` **17/17**:新增两条结构断言 ——
  "搬家成功""子文件夹排在父夹标准回答之前(文件夹优先于内容)";
- `scripts/verify-ui-2026-09-15.mjs` **11/11**:统计断言改为按页签核对
  (记忆 = `问答 n · 回复 m`、文件夹 = `文件夹 k · 标准回答 g`、**设置页为空**)。
- 顺带修:验收脚本里"头部统计"的读取方式由 `body.innerText[1]` 改为
  `header > div:nth-child(2)`,不再依赖整页文本行序。

### 用户真机验收清单(第五轮)
1. 四个页签顶部小字分别只显示本页相关的计数;设置页没有这行字
2. 文件夹页:每个文件夹是一张带边框的卡片,子文件夹在父夹内容之前且缩进;空文件夹显示"暂无标准回答";
   未分类在最下方且为虚线边框
3. 「新建根文件夹」在左侧(与知识库「新建条目」同位置)
4. 面板整体变宽(420px)
5. 切到暗色:开关选中态为蓝色(不再是白色药丸);滚动条悬浮可见;导航图标悬浮有反馈

---

## 〇-C、2026-09-15 第四轮:用户 3 项反馈(全部完成,单测 158/158,tsc 0 错,build 过,验收 9/9 + 14/14)

用户反馈:① 候选面板里已设置的标准回答无法取消;② 已有标准回答时还应能再设其他回答,且最近设置的靠前;
③ 记忆页每个问题的折叠面板没有折叠按钮。

**口径确认(问过用户)**:同一问题的标准回答**合计上限 3 条**(唯一性 = 问题 + 答案,即同问题不同答复可共存,
完全相同的答复不重复建);展示一律**按设置时间倒序**(最近设置靠前)。

1. **数据模型:一问题多条标准回答**(原"一个问题只能一条",靠 questionHash 幂等)
   - 新增 `planGoldenAdd(siblings, input, max)` 纯逻辑(`goldenEdit.ts`):判重 → 判上限 → 建;
     返回 `exists`(同问题同答案,返回已有 id)/ `limit`(已达上限)/ `invalid`。
   - `db.getGoldensByQuestionHash()`:同题全部标准回答,按 createdAt 倒序(候选与面板共用该口径)。
   - `ADD_GOLDEN` 回执扩 `limitReached` / `count`(UI 展示 `已设为标准回答(2/3)` 与上限提示)。
   - 常量 `MAX_GOLDENS_PER_QUESTION = 3` 落在 `types/memory.ts`:后台写入口径与聊天页提示同一个数字。
2. **取消标准回答**(用户反馈"无法取消")
   - 候选弹窗每行改为**状态化操作钮**:`kind='golden'` 进面板即显示「取消标准回答」,点后发 `DELETE_GOLDEN`;
     历史/知识库候选设置成功后**就地翻转**为「取消标准回答」,无需重开面板(危险态悬浮转红)。
   - 取消只解除"标准回答"身份,历史问答记录不受影响(沿用既有删除语义)。
3. **排序:最近设置靠前**
   - `assembleSuggestions` 尾部新增 `groupGoldenAnswersByRecency`:同问题的多条标准回答归一组,
     组内按时间倒序,组位置取该组名次最高成员的位置(修复"同题多条被时间序打散")。
   - `panelLogic.orderGoldensByRecency`:文件夹页同题多条同样按时间倒序;新增
     `countGoldensByQuestion` → 行上标 `同问题 n 条`,达上限追加 ` · 已满`。
4. **记忆页折叠按钮**(用户反馈"目前没有")
   - 卡片头左侧加 24px chevron 折叠钮(折叠旋转 -90°,带 `aria-expanded`,整行仍可点);
     展开区左缩进与问题文本对齐;折叠逻辑抽为 toggle()。
5. **导入导出**
   - `planGoldenImports` 幂等键由 questionHash 改为 **(questionHash + answer)**,并按上限截断
     (新增 `limited` 计数,`ImportOutcome.limitedGoldens` → 设置页提示"超每问题上限 N")。
   - `planGoldenEdit` 语义同步:同题不同答案允许合并(原"问题撞 hash 即拒"作废),
     新增"目标问题已满"与"同题同答案重复"两类拒绝。

验收证据:
- `scripts/verify-ai-popup-2026-09-15.mjs`(把构建产物注入复刻页面 + 桩接管 runtime)9/9:
  标准回答候选显示「取消标准回答」→ 点取消发出 DELETE_GOLDEN 且 id 正确 → 原位翻回「设置标准回答」;
  历史候选设置 → 发 ADD_GOLDEN(问题=气泡文本)→ 就地翻为「取消标准回答」;达上限提示且不误报成功。
- `scripts/verify-golden-multi-2026-09-15.mjs`(真扩展 + SW)14/14:连续 3 条 count 递增;
  第 4 条 limitReached 且未落库;重复答案 exists 幂等;文件夹页顺序 3→2→1 且标「同问题 3 条 · 已满」;
  取消一条后再设成功(额度释放),顺序 5→3→1;清理无残留。
- `scripts/verify-ui-2026-09-15.mjs` 9/9:新增折叠钮用例(aria-expanded 翻转、可复原)。
- **踩坑记录**:持久化 profile 会缓存旧 SW 脚本(第 1 次跑拿到的是上一版口径的返回),
  故两个真机脚本改为**每次运行独立 profile + 跑完即删**(`.diag-run-*`,已入 .gitignore)。

### 用户真机验收清单(第四轮)
1. 聊天页对同一问题:候选面板可连设 3 条标准回答,第 4 条提示"该问题已有 3 条标准回答,请先取消一条"
2. 已是标准回答的候选,操作钮为「取消标准回答」,点后该条不再参与标准回答,历史记录仍在
3. 文件夹页:同题多条按最近设置靠前,行上标「同问题 n 条 / 已满」
4. 记忆页:每条问题左侧有折叠箭头,点击折叠/展开

---

## 〇-B、2026-09-15 第三轮:用户 4 项反馈(全部完成,单测 138/138,tsc 0 错,build 过,几何验收 13/13)

用户反馈:① AI回复按钮与气泡重叠、按钮不符合设计风格且偏矮;② 文件夹页创建子文件夹的输入框与按钮不等高,
顺带排查其他地方;③ 删除导航栏顶部 logo;④ 记忆页点击「设置标准回答」无效、没反应。

**先用数据定位,不靠肉眼**:自写零依赖 PNG 解码器(`scripts/measure-clip-pixels.py`,环境无 PIL)量用户截图 ——
- 截图 1:气泡白块右缘 x≈100、按钮左缘 x≈102 → **视觉间距仅 1~2px**;气泡高 40px、按钮高 22px → 按钮明显偏矮。
- 截图 2:输入框上/下边框 y=45/y=79 → **35px**;「创建」黑钮 y=45→72 → **28px**,差 7px。与用户描述完全一致。

1. **AI回复按钮压气泡(真因是锚点选错,不是间距不够)**
   真机结构 `li.onemsg > .buyer-item > div[currentuid] > .msg-content > p.msg-content-box`:
   气泡底色与内边距在上层容器上,`<p>` 的 rect 右缘落在气泡内边距之内(实测短 ~10px)。
   上一轮把间距从 8 调到 12,但按 `<p>` 右缘量,扣掉内边距后仍只剩 ~2px —— **调数字治不了根**。
   改为**锚气泡真实外缘**:自文本块向上吸收「有不透明背景色 且 宽度贴近文本块(容差 48px)」的最外层祖先,
   宽度守卫排除整行容器的底色;兜底取文本块直属容器。算法抽到 `src/utils/pddBubbleAnchor.ts`(+8 单测)。
2. **AI回复按钮尺寸对齐设计系统**:20px/4px 圆角/11.5px 字号 → **26px(controlH.form)/6px(md)/12.5px(body)**,
   `box-sizing: border-box` 显式声明(覆盖层注入平台页面,不享 popup 的全局重置);间距 12px 按真实气泡外缘计。
3. **同行控件等高铁律**:新增 `design.ts#controlH = { inline: 24, form: 26 }` 令牌 + `components.tsx#controlStyle()`;
   用**显式 height** 替代原来靠 `padding + line-height` 撑(后者随字号/行高漂移)。
   全仓排查同类问题并一并修正:内联表单(新建根/子文件夹)输入框 35→26px 与按钮齐平;
   分区头重命名输入框 24 与 BtnMini 22 → 双双 24;内联确认行按钮 22→24;迁移下拉 22→24;
   `.pddcs-btn` 高度固定 26px(原靠 padding 隐式撑,已 26 但不确定);工具栏入口钮 26→26。
4. **删除导航栏顶部品牌标**(AiSparkIcon 蓝底方块):导航栏首项即「记忆」页签。
5. **记忆页「设置标准回答」的可见反馈**(写入其实一直是成功的)
   真机复现(`scripts/verify-ui-2026-09-15.mjs`):点击后 goldenCount 0→1,**写入没坏**;坏在反馈不可见 ——
   提示条渲染在列表顶部(长列表/滚动后在视口外)、头部统计不自刷新、列表本身也不会变 → 界面毫无动静。
   三重补救:① 按钮原位「设置中…」→「已设为标准回答」(绿色对勾,最贴近点击点);
   ② 成功即 `onDataChanged()` 刷新头部统计;③ `Notice` 改为**吸附滚动区顶部**(`position: sticky`,加 `tk.bg` 底板承载半透明成功色)。
6. **文档**:DESIGN.md → v2.2(控件等高铁律与 controlH 令牌、Notice 吸附、气泡锚点算法、AI 按钮规格)。

验收证据:
- `scripts/verify-ui-2026-09-15.mjs`(真机扩展 + 合成问答)6/6:按钮变已设 / 统计 2→3 / logo 0 命中 /
  输入框与两按钮均 26px 且顶边对齐 / 行内小控件 24px / 无页面错误;截图 `logs/ui-0915-{memory,folders}.png`。
- `scripts/verify-ai-button-geometry.mjs`(把构建产物注入复刻 DOM 夹具,量真实几何)13/13:
  按钮 62.3×26.0、6px 圆角、12.5px 字号、border-box;到气泡可视外缘 12.0px(旧口径只有 2px);垂直居中偏差 0.3px。

### 用户真机验收清单(第三轮)
1. chrome://extensions 重新加载扩展 + 刷新聊天页;「AI回复」按钮应明显变大(26px),与气泡之间留出清晰空隙
2. 文件夹页 → 文件夹行悬浮 → 子文件夹图标 → 输入框与「创建/取消」严格等高;重命名同理
3. 导航栏顶部不再有品牌标
4. 记忆页点击「设置标准回答」:按钮当场变绿色「已设为标准回答」,顶部统计 +1,顶部绿条提示

---

## 〇-A、2026-09-15 第二轮:pddddd 工具风对齐 + 文件夹模块重构(完成,单测 131/131,tsc 0 错,build 过)

用户反馈:文件夹模块乱 / 圆角太大 / 上轮 Figma 风格不对,要求参照 `E:\个人项目\pddddd` 重设计。
调研结论:pddddd 控制台(styles.css)自称「Figma 式工具设计系统」= **Figma 编辑器 UI 风**
(灰画布白面板/1px 细边框/圆角 4-6-8px/黑白主色/13px 高密度),上轮套的其实是 Figma 营销官网风(胶囊大圆角)。
联网调研文件夹模块主流做法(Chrome 书签管理器/Balsamiq 树指南):两栏 master-detail,
400px popup 本地化为**单栏可折叠分区树 + 行悬浮操作**(恰为 pddddd doc-list 模式)。

1. **令牌对齐 pddddd**:`radius` 8/12/14/16 → **4/6/8/8**(pill 仅剩开关/圆点/计数徽标);
   fontFamily Inter 优先 → **Segoe UI/雅黑**;浅色 text #161616、muted #5C5C5C、tertiary #8C8C8C、
   border #E5E5E5、input 描边 #D4D4D4、bgSecondary #FAFAFA;**主按钮蓝底→黑底白字 #161616**(深色反转白底黑字);
   success #14AE5C / error #F24822 / shadow `0 4px 16px rgba(0,0,0,.08)`;语义色 golden #B8860B/#FDF6E3、
   knowledge #14AE5C。Figma 蓝 #0D99FF 仅剩焦点/滑杆/品牌标。
2. **组件与全局 CSS**:Badge 胶囊→4px 方标(padding 1px 7px);Card 边框 borderLight→border;
   Toggle 选中态 accent→黑(tk.text);RESET_CSS 按钮 9999px→6px/输入框 12px→6px/导航钮 10px→6px;
   新增 `.pddcs-row-ops` 行悬浮显现规则(:hover/:focus-within);pdd-ai-button.ts AI 钮/徽标/迷你钮→4px、
   toast→6px,徽标色改引 semantic 令牌(消硬编码)。
3. **FoldersTab 设计师重构**(布局结构可自由调整,用户已授权):卡片墙→**分隔线行**;
   根文件夹可折叠分区(chevron+图标+名称+圆形计数徽标,未分类置底锁操作);子文件夹 1px 引导线缩进;
   标准回答行:问题加粗单行+回复灰色两行+「填充」黑色主钮常驻,复制/编辑/迁移/删除悬浮显现
   (迁移点击后原位变下拉,失焦取消;删除走常驻内联确认行);新建/重命名内联表单(Enter/Esc)。
   数据流与全部功能零变更。
4. **文档**:DESIGN.md 重写 v2.1(新增 §七 文件夹模块布局章节)。

验证:vitest 131/131 ✓、tsc 0 错 ✓、build ✓、bundle 扫描(#161616/#b8860b/Segoe UI/row-ops 均在,
Inter 仅剩 Internal* 等内部标识符,字体已清)✓。

### 用户真机验收清单(第二轮)
1. chrome://extensions 重新加载;popup 应为工具风:白面板细边框、直角小按钮(主操作黑底白字)、圆角明显变小
2. 文件夹页:分区可折叠、计数徽标、行悬浮才见复制/编辑/迁移/删除图标;「填充」常驻;未分类在最后
3. 聊天页 AI 回复按钮变直角小控件;候选弹窗/徽标/均为小圆角
4. 深色模式切一遍:主按钮应变白底黑字

## 〇、2026-09-15:用户 4 项反馈(全部完成,单测 131/131,tsc 0 错,build 过)

1. **命名更换**:界面「金标准」→「标准回答」、「设金」→「设置标准回答」(popup 四页签+聊天页弹窗+后台 2 条冒泡错误文案)。
   仅改用户可见文案;GoldenRecord/ADD_GOLDEN/kind:'golden' 等内部标识符与数据协议零变更。
   顺手修 KnowledgeTab 层级文案过期(标准回答>历史记录>知识库,原误写知识库在历史前)。
2. **chunk 截断点吸附**(TDD +6 例):chunkText 500 字窗口末端向前吸附到最近空行(\n\n)或「。」之后,
   最小块长 250(CHUNK_MIN_CHARS)防碎片,无边界保持硬切;groupLines 分组截断点吸附空行(块尾不再残留半截空行)。
   重叠语义不变(下一窗起点 = 截断点 − 75)。verify-kb-doc 断言基于小节数与无边界数字串,不受影响。
3. **设计风格切 Figma 官网语言**:theme.ts 换 Figma 配色(蓝 #0D99FF 主操作/文本 #1E1F21/表面 #F5F5F5/边框 #E6E6E6;
   深色 #1E1F21/#2C2C2C、蓝提亮 #4CB3FF);design.ts 新增 fontFamily 令牌(Inter 优先,离线回退,不联网加载);
   知识库徽标换 Figma 绿 #0E8A50;pdd-ai-button.ts 覆盖层 CSS 改为令牌插值(与 popup 同源);
   docs/DESIGN.md 重写 v2.0 设计标准。组件库签名不变,经令牌自动换肤。
4. **AI回复按钮**:删 sparkles 图标与旋转动画(纯文字胶囊);高 26→20px、字号 11.5、padding 0 10px;
   气泡间距 8→12px(用户反馈重叠),垂直居中偏移 -13→-10、底部夹 -28→-22,预估宽 80→70。

提交拆 4 个(吸附/改名/Figma/按钮);远程改指 pdd-rag-plugin.git。**注意:生产 CSS 模板是运行时插值,
bundle 中保留 `${lightTheme.xx}` 引用属正常,验收看渲染效果别 grep 字面色值。**

### 用户真机验收清单(2026-09-15 第一轮;视觉项已被第二轮工具风取代,见 §〇-A)
1. ~~chrome://extensions 重新加载扩展;popup 视觉应为 Figma 风(蓝主按钮/白+浅灰)~~(→ 工具风,见 §〇-A)
2. 全界面无「金标准」「设金」字样;记忆页回复行按钮为「设置标准回答」;聊天页弹窗设金按钮为「设置标准回答」
3. 上传含小节+空行的长 md → 块边界落在段落间隙/句号处,检索命中片段语义完整
4. 聊天页买家气泡右侧「AI回复」纯文字小按钮(20px),不压气泡;点击检索正常



## 一、项目目标(一句话)

本地 Chrome MV3 扩展:在拼多多商家聊天工作台捕获 买家问题+客服回复 → 本地向量库;买家消息旁提供 AI 回复按钮(混合检索→填充官方输入框,人工点击发送);多回复时弹 top-3 供选;可提升金标准(独立长期存储);数据只存本地 IndexedDB,绝无外传;绝不自动发送。

## 二、当前状态:P4-KB 知识库 v1 + md 文档上传 ✅ 实现完成(待用户真机验收)

### md 文档上传完成明细(2026-09-08 深夜,用户需求:上传 md→自动分块→向量化,chunk 逻辑同原项目)
- **分块** `utils/chunkText.ts`:原项目 `chunkText` 原样移植 —— **500 字滑动窗口 / 75 字重叠(步长 425)**,≤500 整段返回;+6 例单测(常量/边界/重叠/覆盖)
- **数据** KnowledgeRecord 加 `source: 'manual'|'doc'` + `docId`;db **version(3)** knowledge 加 docId 索引;DAO `listKnowledgeByDoc`/`deleteKnowledgeByDoc`
- **编排** `knowledge.importKbDocument`:文档名去扩展名 = docId;**同名整篇替换**(先删旧块再建);块 id `${rootId}-c{i}`(原项目同风格)、标题"文档名 · 段n";**锚向量 = 块正文**(区别于手工条目的标题锚);上限 10 万字符提示拆分
- **检索** search.ts 按 source 区分:doc 块 BM25 与来源摘要均走正文(与向量锚一致)
- **防线** 文档块拒 UPDATE_KB 直接编辑(改标题会嵌错文本),UI 隐藏编辑按钮,要改重传整篇
- **UI** 知识库页"⬆ 上传 .md"按钮(文件读取→UPLOAD_KB_DOC);文档块徽标"📄 文档";候选弹窗来源标签区分 原问题/来源
- **导入导出** ExportedKnowledge 透传 source/docId(显式挑字段),文档块随信封搬库
- **验收** `scripts/verify-kb-doc.mjs` **9/9**:上传 1560 字 md → 4 块(公式逐一比对含重叠)→ 全块向量回填 → 检索命中块(score 0.66,来源摘要=块正文)→ 编辑被拒 → 重传替换(4→4 无重复)→ 导出含 source/docId → 幂等再导入 → 清理;单测 **116/116**;tsc 0 错;verify-kb v1 回归见下


### P4-KB 完成明细(2026-09-08 深夜)
- **检索第三源**(TDD 先红后绿,+5 例):`retrieval.ts` SourceKind 加 'knowledge';**知识库与金标准同走 goldenThreshold(放宽 0.4)**——均为人工精选源;`assembleSuggestions` 加可选 `getKbContent`(缺省视为空,不破坏既有调用);层级原为 **金标准 > 知识库 > 历史**,**2026-09-09 用户反馈后改为 金标准 > 历史 > 知识库**(见 §七);折叠组内胜者同步
- **纯逻辑** `knowledgeEdit.ts`(+5 例,与 goldenEdit 同构):空字段拒 / 标题冲突拒 / **仅标题实质变更才 reembed**(检索锚=标题向量);正文编辑不动向量
- **数据层** `db.ts` version(2) 新表 knowledge(索引 questionHash/hasEmbedding/enabled)+ DAO;getStats 加 knowledgeCount;markEmbeddingFailed 扩三 kind
- **嵌入链** `offscreen.queueEmbedding` / `syncEmbeddings` 扩 kind 'knowledge'(锚=标题);三源交替补嵌 金→知→史
- **编排** `knowledge.ts`:createKnowledge(标题 hash 幂等,exists 返回)/ updateKnowledgeWithReembed(enabled 切换不重嵌)/ deleteKnowledge;`panel.getPanelData` 加 knowledge(剥向量);`transferPlan`/`transfer` 信封 v2 加 `knowledge` 字段(**始终导出,不受 includeMemory 门控**,enabled 保留、向量剥离导入重嵌)
- **消息与路由** messages.ts +CREATE_KB/UPDATE_KB/DELETE_KB +PanelKnowledge +ImportData 计数;SW 三路由
- **UI**:popup `KnowledgeTab.tsx`(新建内联表单/关键词筛选/编辑/停用开关(停用不参与检索不重嵌)/删除内联确认/填充/复制,teal 徽标 #10b981)替换占位页;头部统计加知识数;设置页导入提示加知识计数;content 弹窗加"知识库"徽标+填充 toast 文案
- **验收** `scripts/verify-kb.mjs` **19/19**:新建幂等→向量回填(首跑含模型下载)→检索命中 knowledge(score 0.88)→改标题重嵌→命中新版→仅改正文不重嵌→空标题拒→停用不命中/启用恢复→同锚金标准+知识库层级 golden>knowledge→导出剥向量→幂等再导入→删除后不命中
- **回归**:P3 合成验收 21/21 ✅;settings 探针 PASS;单测 **110/110**;tsc 0 错

### 本轮用户反馈修复(2026-09-08)
1. **直接填充开关自动关闭**(用户报告):根因 = 设置页改动只存 draft、需手动点「保存设置」才落库,popup 失焦即丢 → 改**自动保存**(开关即时落库,滑杆 500ms 防抖;persist 内 clearTimeout 防旧值回写),移除保存按钮;真机探针 `scripts/verify-settings.mjs`(真实 popup 勾选→直接读 storage 断言)PASS
2. **记忆列表默认展示问题与回复**(用户要求):`MemoryListTab` 折叠语义反转——默认全展开,记录手动折叠项(`collapsedIds`)

### KB 用户真机验收清单(待用户在自己 Chrome 执行)
1. chrome://extensions 重新加载扩展
2. popup 知识库页签:新建条目(标题+正文)→ 出现"向量生成中…"片刻后消失
3. **上传 .md 文档 → 提示"已导入《xx》:N 块,后台逐块向量化";列表出现"📄 文档"徽标、标题带段号的只读条目(无编辑按钮)**
4. 聊天页问文档里的问题 → 候选弹窗出现"知识库"徽标候选,来源行显示命中片段;点击填充→手动发送
5. 编辑手工条目标题保存 → "正在重新生成标题向量…";改用新标题问 → 命中新版
6. [停用] → 检索不再命中;[启用] → 恢复;[删除] 内联确认生效;同名 .md 重新上传 → 提示"(替换旧版)"
7. 设置页导出 JSON → 文件中含 knowledge 数组;再导入 → 提示知识 +0(跳过 ≥1)

### P3 完成明细(2026-09-08 深夜)

### P3 完成明细(2026-09-08 深夜)
- **P3-1 纯逻辑**(TDD 先红后绿,+32 例共 96):
  - `utils/panelLogic.ts`:关键词筛选(归一化包含)/ 剩余保留天数(向上取整夹 0)/ 两层文件夹树(孤儿金标准归"未分类"、悬空子夹当根层、"未分类"缺失时合成兜底节点仅在确有孤儿时出现)
  - `background/transferPlan.ts`:信封 v2 构建(统一剥向量、hasEmbedding=0,导入端全量重嵌)/ 金标准按 questionHash 幂等 / 文件夹按 id 幂等+悬空父置 null / 记忆搬库(自检数据排除、回复孤儿丢弃计数)
  - `background/goldenEdit.ts`:编辑决策(空字段拒/同 hash 冲突拒/**问题实质变更才 reembed**——v1 检索锚是问题向量,答案编辑不动向量)
- **P3-2 后端与消息**:messages.ts +14 组消息(GET_MEMORY_LIST/DELETE_QA/GET_PANEL_DATA/UPDATE_GOLDEN/DELETE_GOLDEN/CREATE_FOLDER/RENAME_FOLDER/DELETE_FOLDER/UPDATE_SETTINGS/EXPORT_DATA/IMPORT_DATA/FILL_INPUT);`background/panel.ts`(记忆列表含回复展开、面板数据剥向量减体积、文件夹 CRUD 两层约束+"未分类"保护);`background/transfer.ts`(版本校验→计划→bulkAdd→排队重嵌→设置合并;导入悬空 folderId 归未分类);goldens.ts updateGoldenWithReembed + 提升默认入"未分类";SW handleFillInput(tabs.query 找聊天页 → tabs.sendMessage 转发);content PDD_FILL_INPUT 监听
- **P3-3 popup 四页签重写**(记忆/文件夹/知识库占位禁用/设置;P0 自检卡按设计移除):金标准卡五操作——填充(SW 转发聊天页)/复制/编辑(双字段表单,保存即重嵌)/迁移(下拉选夹)/删除(内联二次确认);⚠ **Chrome 116+ 扩展 popup 禁用 window.alert/confirm/prompt** → 所有确认/编辑一律内联 UI
- **P3-4 合成验收**(`scripts/verify-p3.mjs`,一次性 profile):**21/21 通过**——捕获→设金幂等→重嵌→检索命中金标准(score 0.78 置顶)→编辑重嵌→命中新版→仅改答案不重嵌→空编辑拒→两层约束→迁移→导出剥向量→原样再导入全跳过→坏版本拒绝→FILL_INPUT 无聊天页报错→清理测试数据。导出→清库→导入恢复的"清库"一步留给用户真机自选执行(脚本不碰真实库)

### ★ P3 排查坑(并入教训清单)
1. verify-p3 首跑 GET_MEMORY_LIST 空响应 → 登录态 profile **SW 陈旧**(P1 教训 #1 重演);补强制 reload 后 **profile 直接起不来扩展**(P1 教训 #2 重演且加重)→ 改一次性 `.diag-fresh-profile`(纯数据层验收无需登录态),重嵌轮询预算放大 ~5 分钟(首跑含模型下载 ~25MB)
2. **Chrome 116+ popup 无原生对话框**:alert/confirm/prompt 全部失效,面板必须内联交互
3. 导入计划函数禁止 `{...rest}` 展开外来记录(运行时多余字段会混入),必须显式挑字段

### P2 完成明细(2026-09-08 深夜,已验收)
- **P2-1 检索层**(`background/retrieval.ts`,纯逻辑 17 例单测):余弦(点积)/中文 bigram 分词/BM25/RRF(k=60)/半衰期 30 天时间衰减/双阈值过滤(金 0.4 宽、史 0.5)/回复展开→hashText 折叠→金标准置顶→截断 10
- **P2-2 检索编排**(`background/search.ts` + `GET_SUGGESTIONS` 路由):查询向量(offscreen)→ 双源读库 → rankCandidates → 组装;响应附 settings 快照(UI 状态机用,免 content 读 storage)
- **P2-3 真机校准**(`scripts/probe-input.mjs`,logs/input-calib.log):
  - 输入框 = 原生 `textarea#replyTextarea`;填充用**原生 value setter + input 事件**(写入/清空真机验证过)
  - 买家行 = `li#middlePanel_list_<ms> > .buyer-item > [currentuid] > .msg-content > p.msg-content-box`;**行 rect 可滚出容器(y 为负)**→ 按钮必须按可见性裁剪
- **P2-4 content UI**(`contents/pdd-ai-button.ts` + `utils/pddUiLogic.ts`):
  - body 下 fixed 覆盖层(不进 Vue 子树,防重渲染清除);每个**可见**买家行右侧 22px 半透明 AI 圆钮;800ms 重排 + scroll 捕获/resize
  - 点击 → 向上合并连续买家行为 query(保留尾部 800 字)→ GET_SUGGESTIONS → 状态机(`decideUiAction` 单测):无候选 toast / 直填开关开或单候选 → 直填 / 多候选 → 弹窗 top-3
  - 弹窗:金标准徽标置顶 + 得分 + 同内容×n + 原始问题 + [设金]/[复制];Esc/点外部关闭
  - [设金] → `ADD_GOLDEN` → `background/goldens.ts`(normalize+questionHash 幂等,queueEmbedding)
  - 填充只写输入框 + toast「请手动发送」;**全链路无任何自动发送**
- **自动化真机验收**(`scripts/verify-p2-ui.mjs`,logs/verify-p2-ui.log):按钮渲染 2 个 ✓ → 点击检索 → 弹窗 3 候选 ✓ → 点候选 textarea 真实填充(历史回复全文)✓ → 消息行数 11→11 未发送 ✓ → 清空 ✓

### ★ P2 排查中抓到的工程坑(重要)
**`npm run build:prod` 一直在写错目录**:`--target=chrome-mv3-prod` 不是合法 target,Plasmo 把它拼成 `build/chrome-mv3-prod-prod`;而验收脚本/用户 Chrome 加载的 `build/chrome-mv3-prod` 是旧产物 → 新 content script 丢失、按钮 0 个。已改 `build:prod` = `plasmo build`(默认输出 `build/chrome-mv3-prod`)。**以后怀疑"改了没生效",先 `ls build/chrome-mv3-prod/*.js` 对产物。**

### P2 测试现状(已并入 P3 现状)
**96/96 单测绿**(tsc 0 错,build 过);P2 贡献:`pddUiLogic` 7 例、`retrieval` 17 例
> 注:P4-KB 后全库 **110/110**(retrieval +5、transfer +4、knowledgeEdit +5),见 §二。

### 用户验收结论(2026-09-08)
P2 全部通过;用户反馈存在若干**设计层面问题,留到最后统一解决**(细目待用户整理,届时一并处理)。

### 下一步
- **KB-5 用户真机验收**(见 §二 KB 清单,6 步)与 **P3-5 用户真机验收**(见 §五 P3 清单)
- P4 打磨:页面改版抗性巡检、阈值手感调优、按钮重排节流(行多时)、README 对齐
- **挂账:用户反馈的若干设计问题留到最后统一解决**(细目待用户整理)

## 三、P1 捕获链路 ✅ 全部完成并真机验收通过

### P0 工程底座 ✓
Dexie schema / bge-small-zh-v1.5 嵌入链路(offscreen,512 维)/ popup 统计 / SW 路由 / 设置 / TTL。

### DOM 捕获链路 ✓(2026-09-08)
- 网络层路线已探明并**放弃**(消息走 XHR 轮询非 WS,MAIN hook 脆弱),改 **DOM 观察路线**(用户拍板)
- `contents/pdd-chat-capture.ts`(ISOLATED,观察 `#msgListContainer`,MutationObserver+2.5s 兜底轮询,行级去重,批量 PDD_INGEST,会话切换/页面卸载发 leave)
- `utils/pddDomParser.ts` 纯函数提取;`background/pddSegmenter.ts` 分段状态机;`background/pddCapture.ts` 落盘胶水

### ★ P1 落盘为 0 阻塞问题:已定位修复(2026-09-08 晚)
**根因**:会话键字段错位 —— content script 把 `sessionKey`/`buyerIdTail` 放在**事件级**(`ev.sessionKey`),而 `pddCapture.ts` 对 msg 事件只读**消息级**(`m.sessionKey`,`PddCapturedMsg` 上恒 undefined)→ 所有 DOM 消息被判 `nosession` 跳过,分段器从未执行到(content script 从不发 `active`,`tabSessions` 兜底永远为空)。
**修复**:`pddCapture.ts` 改读 `ev.sessionKey ?? m.sessionKey`(及 buyerIdTail 同样兼容),`pddSegmenter.SegMsg` 增加 `buyerIdTail` 透传到问答记录。
**验证**:新增 `tests/unit/background/pddCapture.test.ts` 3 例(TDD 先红后绿);40/40 单测过;tsc 0 错;真浏览器合成链路 qaCount 0→1、msgId 幂等(`dupmsgid`)正常、错误表干净;**用户在自己 Chrome 真机验收:问答记录正常增长** ✓

### 排查期曾误导方向的两个环境坑(重要教训,以后遇到先查这两条)
1. **SW 跑陈旧代码**:rebuild 后浏览器可能仍执行旧 SW 脚本(响应缺新增字段即证据,如无 `detail`)。登录态 profile 尤其顽固,重启浏览器不够;对策:诊断脚本先 `chrome.runtime.reload()` 或让用户 chrome://extensions reload。`scripts/diag-sw-version.mjs` 内置"版本探针"(无 sessionKey 事件 → 新代码应答 `detail:"nosession=1"`)+ login 模式自动 reload。
2. **脚本异常退出污染 profile**:诊断脚本崩溃(未 `ctx.close()`)后,该 profile 的扩展 SW 可能从此起不来(重启也不行)。`.chrome-debug-profile` 已处于此状态(**未修复**,自动化测试改用一次性 profile:`diag-sw-version.mjs` 默认 `personal-ai-memory/.diag-fresh-profile`,污染了就删掉重跑)。人工修复尝试 `scripts/repair-ext.mjs`(chrome://extensions 的 developerPrivate reload),曾卡在 launch 未验证完。
3. 附:SW 内 `chrome.runtime.sendMessage` **不会回送到自身 onMessage**——合成测试必须从扩展页(popup)发,不能 `sw.evaluate` 里发。

## 三、数据链路(现状,已验证)

```
content(pdd-chat-capture,ISOLATED) ──PDD_INGEST{events}──▶ SW
  handlePddIngest: 会话解析(ev.sessionKey ?? m.sessionKey,缺省 tab 兜底)
    → normalize → 保留期过滤 → msgId 幂等(内存 Set,SW 唤醒即清)
    → PddSegmenter(买家连续文本=问题段,客服文本关闭落盘+挂回复;idle/leave 关无回复段)
    → Dexie(QaRecords/Replies) + queueEmbedding(问题向量,offscreen)
```
- 段内异常 → `db.logError()` 落 `errors` 表(常驻:SW 控制台生产不可达,DB 是唯一观测通道);跳过原因计数随响应 `detail` 返回(保留,开销可忽略)
- content 逐行捕获日志已降级到 `DEBUG=false` 开关(`pdd-chat-capture.ts`)

## 四、关键事实(避免重复探索)

### 测试环境
| 项 | 值 |
|---|---|
| 浏览器 | `C:\Users\胡起嘉\AppData\Local\ms-playwright\chromium-1223\chrome-win64\chrome.exe` |
| 登录 profile | `E:\个人项目\拼多多客服检索工具\.chrome-debug-profile`(**P3 期间再次污染,扩展起不来;自动化一律用一次性 `.diag-fresh-profile`,真机验收在用户日常 Chrome**) |
| 扩展 build | `build\chrome-mv3-prod`(plasmo build;**rebuild 后必须在浏览器里 reload 扩展**) |
| 聊天测试会话 | E***E,买家 uid `1875210170836`(店:眼睛的店铺 / mall 515205963) |

### 聊天页 DOM 结构(2026-09-08 真机校准,Vue2+data-v,全语义类名)
```
#msgListContainer.history-box > ul.msg-list > li.onemsg[id="middlePanel_list_<毫秒号>"]
  > div.merchantMessage
      > p.message-time?(分组时间,可缺)
      > div.cs-item[.isread][.callback]  客服(含机器人回复 callback 行)
      > div.buyer-item                    买家
      > div.system-msg-31 等              系统行(跳过)
    > 角色容器内 div[currentuid="<买家uid>"] > .msg-content > p.msg-content-box  ← 唯一正文源
```
- 会话键 = `currentuid` = 买家 uid;时间用 `.message-time` 或行 id(ms)兜底;图片/商品卡无 `.msg-content-box` → 跳过
- 滚动可触发更早历史渲染(MutationObserver 覆盖);会话可能被机器人接管,消息区要点开+滚动才渲染

### 合规边界(持续有效)
只读 DOM + 只填官方输入框(人工点击发送);数据不出本机;模型下载仅 hf-mirror.com;不点平台"恢复接待"之类外部动作。

## 五、遗留与下一步

### 遗留(不阻塞,记录在案)
- SW 休眠(约 30s)丢内存态(seenMsgIds/未结段)→ 跨唤醒重复靠 Dexie contentHash + 5min 合并窗兜底
- `findReplyByMsgId` 全表过滤(无索引)——v1 回复不参与向量,量大后再说
- `.chrome-debug-profile` 污染未修(自动化已绕开;要修可跑 repair-ext.mjs 或手动 chrome://extensions)

### P3-5 用户真机验收清单(待用户在自己 Chrome 执行)
1. `chrome://extensions` 重新加载扩展
2. 点扩展图标 → 四页签:记忆 / 文件夹 / 知识库(占位"后续版本开放")/ 设置;头部统计正常
3. 记忆页:最近问答按时间倒序;关键词筛选;点击条目展开回复 → 回复行 [设金];[删除此条] 内联二次确认
4. 文件夹页:新建根文件夹/子夹(三层会被拒);[未分类] 不可改名/删除
5. 金标准卡:[编辑] 改问题保存 → 出现"正在重新生成问题向量…";[迁移] 下拉换夹生效
6. **开着聊天页**,文件夹页点 [填充] → 切到聊天页,输入框已出现该回复 → 手动发送
7. 设置页开"直接填充"保存 → 聊天页点圆钮 → 不弹窗直填最高分
8. 设置页导出 JSON(可勾"含记忆")→ 再点导入选同一文件 → 应提示全部跳过(幂等)
9. 全程确认:任何操作都不会自动点发送/模拟回车

### P2-5 用户真机验收清单 ✅ 已全部通过(2026-09-08,留存备查)
1. chrome://extensions 对本扩展点"重新加载"(SW 必须吃到新 build)
2. 打开 chat-merchant,进入有历史消息的会话 → 买家消息右侧应出现半透明 AI 圆钮(悬停加深)
3. 点击圆钮 → 库中有相似问答时应弹候选(或直接填充);无相似时 toast"未找到匹配"
4. 弹窗点候选 → 输入框出现该回复文本 + toast"请手动发送";**确认不会自己发出去**
5. 弹窗 [设金] → toast 成功;再搜同一问题,该金标准应置顶带金色徽标(放宽阈值 0.4)
6. popup 设置里开"直接填充" → 再点圆钮 → 不弹窗直接填充最高分
7. [复制] 按钮 → 剪贴板有文本
8. 全程观察:任何情况下扩展都不得点击发送按钮/模拟回车

### 脚本现状(scripts/)
- 常用:`e2e-capture.mjs`(真机链路验收)、`diag-sw-version.mjs`(SW 版本探针+合成链路,默认一次性 profile)、`diag-popup-send.mjs`(popup→SW 合成)
- 归档:`scripts/archive/probe-dom*.mjs`(DOM 校准,已完成使命)
- `logs/dom-calib*.log`:校准原始输出,可归档

## 六、文件地图(增量)

| 文件 | 变化 |
|---|---|
| src/utils/chunkText.ts | ★P4-KB 文档 新:原项目 chunkText 移植(500字/75重叠);09-09 起仅作无结构兜底 |
| src/utils/mdText.ts | ★09-09 新:mdToPlainText 净化 + chunkMarkdown 结构感知分块 |
| src/background/kbAnchor.ts | ★09-09 新:知识锚文本唯一出处(嵌入/BM25/摘要同源) |
| src/background/knowledge.ts | ★P4-KB 新:知识库 CRUD + importKbDocument(结构分块/整篇替换) |
| scripts/verify-kb-doc.mjs | ★P4-KB 文档 新:文档上传合成验收(09-09 重写为 11 步) |
| src/background/knowledgeEdit.ts | ★P4-KB 新:编辑决策纯逻辑 |
| src/popup/KnowledgeTab.tsx | ★P4-KB 新:知识库页(七操作) |
| src/background/retrieval.ts | ★P4-KB 扩:knowledge 第三源/层级三级 |
| src/background/db.ts | ★P4-KB 扩:version(2) knowledge 表+DAO |
| src/background/search.ts | ★P4-KB 扩:三源读库 |
| src/background/offscreen.ts / syncEmbeddings.ts | ★P4-KB 扩:kind 'knowledge' |
| src/background/transferPlan.ts / transfer.ts | ★P4-KB 扩:信封 knowledge 字段(始终导出)+导入幂等 |
| src/contents/pdd-ai-button.ts | ★P4-KB 扩:知识库徽标/toast |
| scripts/verify-kb.mjs | ★P4-KB 新:合成验收(19 步) |
| scripts/verify-settings.mjs | ★新:直填开关落库真机探针 |
| src/popup/SettingsTab.tsx | ★修复:自动保存(开关即时落库/滑杆防抖);导入提示含知识计数 |
| src/popup/MemoryListTab.tsx | ★修复:默认展开问题与回复 |
| src/types/memory.ts / messages.ts | ★P4-KB 扩:KnowledgeRecord/PanelKnowledge/KB CRUD 消息 |
| src/background/pddCapture.ts | ★会话解析修复(ev/m 级兼容)+ buyerIdTail 透传 |

## 七、2026-09-09:结构感知分块修复(md 符号泄漏 + 语义截断)

### 问题(用户真机反馈,上传真实《售后政策》md 后)
1. 检索结果含大量 `##`/`-` 等 md 符号 —— chunkText 盲切窗口原样保留标记,填充出去的是裸 md 源码
2. 内容语义不完整 —— 500 字固定窗口在块边界切断列表项(75 字重叠救不回完整性)
3. 用户问是否要接生成模型 → **结论:暂不接**。云端 LLM 违反"数据不出本机"红线;本地生成模型在扩展内太重。
   结构分块+净化已根治两个问题(命中即完整小节、填充即干净话术);生成层(改写为口语回复)留作 v2 议题。

### 方案(确定性,无模型)
- **mdToPlainText**(src/utils/mdText.ts):剥标题/列表/引用/水平线/围栏/行内强调标记、链接保留文字、图片删除;连续空行折叠
- **chunkMarkdown**:按标题行切小节 → 小节 ≤500 字整节成块(语义天然完整)→ 超长小节按行分组**永不截断单行** → 单行超长回退滑窗 → 全文无标题回退原 chunkText(原项目逻辑保留为无结构兜底)
- **块标题** = `docId · 小节标题`(同节拆多块时加"(续n)");**锚文本 = 展示标题+块正文**(小节标题语义强,拼进锚提命中)
- **kbAnchorText**(src/background/kbAnchor.ts):锚唯一出处——嵌入/BM25/来源摘要同源;修复既有隐患(syncEmbeddings 补嵌、transfer 导入原本嵌 k.title,文档块失败重试会嵌错文本)

### 验证
- 单测 124/124(新增 mdText 8 例)、tsc 0 错
- verify-kb-doc.mjs 重写:**11/11**(无 md 残留/列表项完整/标题含小节名/检索命中且填充干净/拒编辑/整篇替换/无结构回退滑窗/导出导入幂等/清理)
- verify-kb.mjs 回归 19/19

### 注意:已上传旧文档需重传
旧版盲切块(带 md 符号)仍留在用户库里;同名重传即整篇替换。用户真机验收时须重新上传《售后政策》。

### 层级调整(用户反馈:打招呼类场景)
- **金标准 > 历史 > 知识库**(原 金>知>史)。理由:知识库(政策文档)很少有打招呼类预设,历史问答覆盖更好;
  知识库阈值仍放宽(0.4)但置顶优先级最低。`foldWinnerRank`:金 0 / 史 1 / 知 2(折叠胜者与 tier 排序同源)
- 单测 +1 例(历史与知识库同分 → 历史置前),125/125;verify-kb 回归 19/19;commit 9900b13 后追加
| src/background/pddSegmenter.ts | SegMsg.buyerIdTail 开段记入 |
| src/contents/pdd-chat-capture.ts | 诊断日志降级 DEBUG 开关 |
| src/background/retrieval.ts | ★P2 新:混合检索纯逻辑 |
| src/background/search.ts | ★P2 新:检索编排(GET_SUGGESTIONS) |
| src/background/goldens.ts | ★P2 新:设金(幂等+排队嵌) |
| src/contents/pdd-ai-button.ts | ★P2 新:AI 按钮+弹窗+直填(fixed 覆盖层) |
| src/utils/pddUiLogic.ts | ★P2 新:query 合并+UI 状态机(纯函数) |
| src/utils/panelLogic.ts | ★P3 新:记忆筛选/剩余天数/两层文件夹树(纯函数) |
| src/background/transferPlan.ts | ★P3 新:导入导出 v2 信封+幂等计划(纯函数) |
| src/background/goldenEdit.ts | ★P3 新:金标准编辑决策(纯函数) |
| src/background/panel.ts | ★P3 新:记忆列表/面板数据/文件夹 CRUD 编排 |
| src/background/transfer.ts | ★P3 新:导出编排+幂等导入+排队重嵌 |
| src/background/goldens.ts | ★P3 扩:编辑重嵌/删除;提升默认入"未分类" |
| src/popup/MemoryListTab.tsx | ★P3 新:记忆列表页 |
| src/popup/FoldersTab.tsx | ★P3 新:文件夹页(金标准卡五操作) |
| src/popup/SettingsTab.tsx | ★P3 新:设置页(开关/阈值/保留期/导入导出) |
| src/popup/ui-bits.tsx | ★P3 新:Notice/Btn/formatTime 共用件 |
| src/popup/index.tsx | ★P3 重写:四页签壳(自检卡移除) |
| scripts/verify-p3.mjs | ★P3 新:数据层端到端合成验收(21 步) |
| src/types/messages.ts | +Suggestion/GET_SUGGESTIONS/ADD_GOLDEN |
| src/background/db.ts | +getEmbeddedQaRecords/getEmbeddedGoldens/getRepliesByQaIds |
| package.json | ★build:prod 修正(原 target 值错写产物目录) |
| tests/unit/background/retrieval.test.ts | 新 17 例 |
| tests/unit/utils/pddUiLogic.test.ts | 新 7 例 |
| tests/unit/background/pddCapture.test.ts | 新 3 例(回归/形态/守卫) |
| scripts/probe-input.mjs | 新:输入框/行结构校准(P2-3) |
| scripts/verify-p2-ui.mjs | 新:P2 端到端真机验收 |
| scripts/diag-sw-version.mjs | 新:版本探针+reload+合成+清理 |
| scripts/repair-ext2.mjs | management.setEnabled 修复扩展禁用(已验证) |
| scripts/archive/probe-dom*.mjs | 校准脚本归档 |
